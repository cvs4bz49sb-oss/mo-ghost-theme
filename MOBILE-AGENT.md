> See `../MOBILE-AGENT.md` (one level up, at the MOCA root). That's the canonical version.

This file is a pointer so commands like `/mobile-check` running from inside the theme repo can find the agent's constitution.
