--- a/tools/prdl_reader_prototype/connection-evidence.css
+++ b/tools/prdl_reader_prototype/connection-evidence.css
@@ -8,16 +8,16 @@
 @media (max-width:640px){.ce-pane{max-height:70vh}}
 
 /* records by citing work (2026-09-10) */
-.ce-work{border-top:1px solid var(--border,#d9d4cb)}.ce-work:first-child{border-top:0}.ce-work>summary{display:flex;align-items:baseline;gap:10px;padding:10px 0;cursor:pointer;list-style:none;font-size:15px}.ce-work>summary::before{content:'+';color:var(--muted,#6b675f);font-family:var(--sans,system-ui);width:12px;flex:none}.ce-work[open]>summary::before{content:'−'}.ce-work>summary>span{flex:1;min-width:0}.ce-n{color:var(--muted,#6b675f);font-variant-numeric:tabular-nums;white-space:nowrap}.ce-open-work{font-size:12.5px;white-space:nowrap}.ce-fold-body{padding:0 0 8px 22px}
+.ce-work{border-top:1px solid var(--border,#d9d4cb)}.ce-work:first-child{border-top:0}.ce-work>summary{display:grid;grid-template-columns:12px minmax(0,1fr);align-items:baseline;column-gap:10px;row-gap:2px;padding:10px 0;cursor:pointer;list-style:none;font-size:15px}.ce-work>summary::before{content:'+';color:var(--muted,#6b675f);font-family:var(--sans,system-ui);width:12px;flex:none}.ce-work[open]>summary::before{content:'−'}.ce-work>summary>span{grid-column:2;grid-row:1;min-width:0;overflow-wrap:anywhere}.ce-work>summary>.ce-n{grid-column:2;grid-row:2}.ce-work>summary::before{grid-column:1;grid-row:1}.ce-work>summary::-webkit-details-marker{display:none}.ce-n{color:var(--muted,#6b675f);font-variant-numeric:tabular-nums;white-space:nowrap}.ce-open-work{font-size:12.5px;white-space:nowrap}.ce-fold-body{padding:0 0 8px 22px}
 
 .ce-records .pinb{font:500 12px/1.2 var(--sans,system-ui);color:var(--muted,#6b675f);border:1px solid var(--border,#d9d4cb);border-radius:3px;background:transparent;padding:3px 7px;cursor:pointer;white-space:nowrap;margin-left:8px}.ce-records .pinb:hover{color:var(--fg,#1b1a17);border-color:var(--fg,#1b1a17)}.ce-records .pinb.on{color:var(--bg,#fff);background:var(--fg,#1b1a17);border-color:var(--fg,#1b1a17)}.ce-records .pinb:disabled{opacity:.6;cursor:wait}.ce-work>summary .pinb-work{flex:none}
 
-.ce-filter-options{margin:12px 0;border-block:1px solid var(--border)}.connection-evidence .ce-filter-options>summary{font:14px/1.5 var(--body);padding:10px 0;min-height:44px;cursor:pointer;overflow-wrap:anywhere}.ce-filter-options .ce-filters{margin:6px 0 16px}.connection-evidence .ce-about>summary{font-size:13px;color:var(--muted);min-height:44px}.connection-evidence .ce-work>summary{flex-wrap:wrap}.connection-evidence .ce-work>summary>span{flex-basis:calc(100% - 60px)}
+.ce-filter-options{margin:12px 0;border-block:1px solid var(--border)}.connection-evidence .ce-filter-options>summary{font:14px/1.5 var(--body);padding:10px 0;min-height:44px;cursor:pointer;overflow-wrap:anywhere}.ce-filter-options .ce-filters{margin:6px 0 16px}.connection-evidence .ce-about>summary{font-size:13px;color:var(--muted);min-height:44px}
 
 /* Fixed reference pages replace thousands of simultaneously rendered records. */
 .ce-pager{display:flex;align-items:center;justify-content:space-between;gap:8px;padding:10px 0;border-block:1px solid var(--border);font:13px/1.4 var(--body)}
 .ce-pager>span{text-align:center;font-variant-numeric:tabular-nums}.ce-pager small{display:block;color:var(--muted);margin-top:3px}
 .ce-work-actions{display:flex;align-items:center;gap:10px 16px;flex-wrap:wrap;padding:0 0 8px}.ce-work-actions a{min-height:44px;display:inline-flex;align-items:center}.ce-work-actions .pinb-work{margin-left:0}
-.ce-records .ce-fold-body{padding-left:0}.ce-records .ce-work>summary{padding:10px 0;min-height:48px}.ce-records .ce-work>summary>span{flex-basis:auto}.ce-records .ce-n{font-size:12px}
+.ce-records .ce-fold-body{padding-left:0}.ce-records .ce-work>summary{padding:10px 0;min-height:48px}.ce-records .ce-n{font-size:12px}
 .ce-record .ce-label{display:inline;color:var(--muted);margin-right:7px}.ce-record .ce-pick{margin:0;min-height:40px}.ce-record p{margin:6px 0}.ce-record{padding:12px 0}.ce-record .ce-source-actions{gap:6px 12px;justify-content:start}.ce-record .ce-source-actions>span{margin-right:auto}.ce-record .pinb{margin-left:0}
 @media(max-width:760px){.connection-evidence{font-size:15px;line-height:1.55}.connection-evidence h4{font-size:19px;margin:0 0 8px}.connection-evidence h5{font-size:21px;margin:16px 0 10px}.connection-evidence h6{font-size:17px;margin:12px 0 8px}.ce-explanation{margin:8px 0}.ce-filter-options{margin:8px 0}.ce-filters{gap:10px}.ce-record{padding:10px 0}.ce-selection{padding:10px 0;margin-top:12px}.ce-fold-body{padding:0}.ce-preview{margin:0}.ce-preview iframe{height:min(480px,62dvh)}.ce-journey-step>summary{font-size:18px;min-height:48px}.ce-pager button{font-size:13px;padding:8px 10px;min-width:72px}.ce-records .pinb{min-height:44px;font-size:13px}.ce-source-actions{font-size:13px}.ce-record .ce-resolution{font-size:12px;line-height:1.5}}
--- a/tools/prdl_reader_prototype/connection-evidence.js
+++ b/tools/prdl_reader_prototype/connection-evidence.js
@@ -82,6 +82,17 @@
     if(row?.resolved?.author||row?.resolved?.author_slug||row?.resolved?.cls==='B')return 'Author identified; held work unresolved';
     return 'Reference not resolved to a held work';
   }
+  // Only reject explicit contradictions between a single Summa question and the
+  // held volume's printed question range. Never guess a replacement target.
+  function targetLocationConflict(row,result){
+    const normalize=value=>String(value||'').replace(/[–—]/g,'-').trim();
+    const title=normalize(result?.work?.title),loc=normalize(row?.loc||row?.locator);
+    const volume=title.match(/^Summa Theologi(?:ca|ae)\s+(I-II|II-II|III|II|I)\s*,?\s*q(?:q)?\.\s*(\d+)\s*-\s*(\d+)\s*$/i);
+    const cite=loc.match(/^(I-II|II-II|III|II|I|1\.2|2\.2|1|3)\s*[.,:]?\s*q(?:uaest(?:io)?)?\.\s*(\d+)(?=\s*[.,]?\s*(?:art\b|a\.|$))/i);
+    if(!volume||!cite)return false;
+    const part={'1.2':'I-II','2.2':'II-II','1':'I','3':'III'}[cite[1]]||cite[1].toUpperCase();
+    return part!==volume[1].toUpperCase()||+cite[2]<+volume[2]||+cite[2]>+volume[3];
+  }
   async function resolvedTargets(row){
     const candidates=[...(Array.isArray(row?.resolved_multi)?row.resolved_multi:[]),...(row?.resolved?.work_slug?[{...row.resolved}]:[])],targets=new Map();
     for(const candidate of candidates){
@@ -156,7 +167,7 @@
     const targetToRestore=resume.filters.target||initialTarget;
     function capture(){return citationViewState({scope:[citing.s,target.s],filters:Object.fromEntries(['work','kind','context','query','target','layer'].map(k=>[k,find('[data-ce-'+k+']')?.value||''])),workPage,recordPages:Object.fromEntries(recordPages),openFolds:host.__ceOpen?[...host.__ceOpen]:null,filterOpen:find('.ce-filter-options')?.open,previews:[...host.querySelectorAll('[data-ce-preview][open]')].map(el=>el.dataset.cePreview)},[citing.s,target.s]);}
     function updateSelection(){const sources=[...selected.values()];find('.ce-selected').textContent=sources.length?count(sources.length)+' selected source passages':'No passages selected';if(find('.ce-save')){find('.ce-save').disabled=!sources.length;find('.ce-save').textContent='Save selected references';}onSelection?.(sources);}
-    function resolutionHTML(identifier){const result=targets.get(identifier);return result?.status==='held'?`<p>Held work recorded by the resolver. The citation attribution and exact passage are not independently verified.</p><a class="ce-read" href="${esc(result.url||readerURL(result.slug))}">Open ${esc(result.work?.title||result.slug)}</a>`:`<p>${identifier?'Author identified · cited work identifier '+esc(identifier)+'; held edition not verified':'Author identified · cited work unresolved'}</p>`;}
+    function resolutionHTML(identifier,row){const result=targets.get(identifier);if(targetLocationConflict(row,result))return `<p class="ce-target-conflict">The recorded target, ${esc(result.work.title)}, does not cover this cited location. Use Open source to check the reference.</p>`;return result?.status==='held'?`<p>Held work recorded by the resolver. The citation attribution and exact passage are not independently verified.</p><a class="ce-read" href="${esc(result.url||readerURL(result.slug))}">Open ${esc(result.work?.title||result.slug)}</a>`:`<p>${identifier?'Author identified · cited work identifier '+esc(identifier)+'; held edition not verified':'Author identified · cited work unresolved'}</p>`;}
     async function locateTargets(){
       const control=find('[data-ce-target]');
       if(!root.FRResearchData?.resolveWork){control.innerHTML='<option value="">Held-work lookup unavailable</option>';return;}
@@ -166,7 +177,7 @@
         const held=[...new Map([...targets.values()].filter(t=>t?.status==='held').map(t=>[t.slug,t])).values()].sort((a,b)=>String(a.work?.title||a.slug).localeCompare(String(b.work?.title||b.slug)));
         control.innerHTML='<option value="">All cited work records</option>'+held.map(t=>`<option value="${esc(t.slug)}">${esc(t.work?.title||t.slug)}</option>`).join('');control.disabled=!held.length;
         if(targetToRestore){const resolved=await root.FRResearchData.resolveWork(targetToRestore);if(!host.isConnected)return;if(resolved?.status==='held'&&held.some(t=>t.slug===resolved.slug)){control.value=resolved.slug;paint();onReady?.();}else control.parentElement.insertAdjacentHTML('afterend','<p class="ce-note">No recorded target in this step resolves to the requested held work. All references are available; a specific quotation or passage has not been located.</p>');}
-        host.querySelectorAll('[data-ce-target-id]').forEach(node=>node.innerHTML=resolutionHTML(node.dataset.ceTargetId));
+        host.querySelectorAll('[data-ce-target-id]').forEach(node=>node.innerHTML=resolutionHTML(node.dataset.ceTargetId,rows[+node.dataset.ceTargetRecord]));
       }catch(_){if(host.isConnected){control.innerHTML='<option value="">Target lookup could not load</option>';const retry=document.createElement('button');retry.textContent='Retry target lookup';retry.type='button';retry.onclick=()=>{retry.remove();locateTargets();};control.parentElement.appendChild(retry);}}
     }
     function paint(){
@@ -176,7 +187,7 @@
       find('.ce-clear').hidden=!activeFilters.length;
       find('.ce-count').textContent=count(hits.length)+(hits.length===1?' available reference':' available references')+(+entry.n>rows.length?' · '+count(entry.n)+' counted in the index':'');
       find('.ce-more').hidden=true;
-      const recordHTML=r=>{const s=source(r,works,citing.a),context=contexts[r.w],layer=sourceContext(r.w,r.p),key=s.url;return `<article class="ce-record" data-ce-record="${r.id}"><label class="ce-pick"><input type="checkbox" data-ce-pick="${r.id}"${selected.has(key)?' checked':''}><span>Select p. ${esc(s.page??"?")}</span></label>${context?`<p class="ce-context">${esc(context.l)}</p>`:''}${layer?`<p class="ce-context">${layer.layer==='editorial-notes'?'Editorial notes from the source edition, not the author’s main text.':layer.layer==='text-and-notes'?'This indexed column has both main text and editorial notes.':'Source notes are available at this indexed location.'} <a href="${esc('/read?w='+encodeURIComponent(r.w)+'&pldpart=3#b'+encodeURIComponent(String(r.p))+'-0')}">Read editorial notes</a></p>`:''}<p><span class="ce-label">Mention</span>${esc(r.sf||'Mention text not supplied')}</p>${r.loc?`<p><span class="ce-label">Cited location</span>${esc(r.loc)}</p>`:''}<div class="ce-note ce-resolution" data-ce-target-id="${esc(r.tw||'')}">${resolutionHTML(r.tw)}</div><div class="ce-source-actions"><span>${r.h?'Recorded as '+esc(r.h)+' · ':''}${s.page!=null?(/^pld-/.test(r.w)?'col. ':'p. ')+esc(s.page):'Location not supplied'}</span><a href="${esc(withHl(s.url,r.sf))}" target="_blank" rel="noopener">Open source</a>${saveBtn(r.w,s.page,s.title,(r.sf||'')+(r.loc?' — '+r.loc:''))}</div>${preview(withHl(s.url,r.sf))}${pageContextHTML(s)}</article>`;};
+      const recordHTML=r=>{const s=source(r,works,citing.a),context=contexts[r.w],layer=sourceContext(r.w,r.p),key=s.url;return `<article class="ce-record" data-ce-record="${r.id}"><label class="ce-pick"><input type="checkbox" data-ce-pick="${r.id}"${selected.has(key)?' checked':''}><span>Select p. ${esc(s.page??"?")}</span></label>${context?`<p class="ce-context">${esc(context.l)}</p>`:''}${layer?`<p class="ce-context">${layer.layer==='editorial-notes'?'Editorial notes from the source edition, not the author’s main text.':layer.layer==='text-and-notes'?'This indexed column has both main text and editorial notes.':'Source notes are available at this indexed location.'} <a href="${esc('/read?w='+encodeURIComponent(r.w)+'&pldpart=3#b'+encodeURIComponent(String(r.p))+'-0')}">Read editorial notes</a></p>`:''}<p><span class="ce-label">Mention</span>${esc(r.sf||'Mention text not supplied')}</p>${r.loc?`<p><span class="ce-label">Cited location</span>${esc(r.loc)}</p>`:''}<div class="ce-note ce-resolution" data-ce-target-id="${esc(r.tw||'')}" data-ce-target-record="${r.id}">${resolutionHTML(r.tw,r)}</div><div class="ce-source-actions"><span>${r.h?'Recorded as '+esc(r.h)+' · ':''}${s.page!=null?(/^pld-/.test(r.w)?'col. ':'p. ')+esc(s.page):'Location not supplied'}</span><a href="${esc(withHl(s.url,r.sf))}" target="_blank" rel="noopener">Open source</a>${saveBtn(r.w,s.page,s.title,(r.sf||'')+(r.loc?' — '+r.loc:''))}</div>${preview(withHl(s.url,r.sf))}${pageContextHTML(s)}</article>`;};
       const box=find('.ce-records');box.classList.add('ce-pane');box.scrollTop=0;
       if(!hits.length){box.innerHTML='<p class="ce-note">No references match these filters. Clear the phrase or choose another work.</p>';return;}
       if(box.__io)box.__io.disconnect();box.innerHTML='';
@@ -247,6 +258,6 @@
       };details.addEventListener('toggle',load);
     });return ()=>{alive=false;};
   }
-  const api={journeyStepFolds,citationViewState,journeyViewKey,pageWindow,citationGroups,esc,fold,source,withHl,readerURL,safeURL,chapterReference,sharedChapters,sharedPages,verseOverlap,citationDirection,directedPath,journeyEdge,journeyConnections,filterCitations,pageContext,resolutionLabel,resolvedTargets,json,preview,bindPreviews,researchNote,save,mountCitation,mountComparison};
+  const api={targetLocationConflict,journeyStepFolds,citationViewState,journeyViewKey,pageWindow,citationGroups,esc,fold,source,withHl,readerURL,safeURL,chapterReference,sharedChapters,sharedPages,verseOverlap,citationDirection,directedPath,journeyEdge,journeyConnections,filterCitations,pageContext,resolutionLabel,resolvedTargets,json,preview,bindPreviews,researchNote,save,mountCitation,mountComparison};
   if(typeof module==='object'&&module.exports)module.exports=api;else root.FRConnectionEvidence=api;
 })(typeof window==='undefined'?globalThis:window);
--- a/tools/prdl_reader_prototype/connection-evidence.test.cjs
+++ b/tools/prdl_reader_prototype/connection-evidence.test.cjs
@@ -115,3 +115,18 @@
  assert.doesNotMatch(save,/onclick=/);assert.match(source,/href="\$\{esc\(withHl\(s.url,r.sf\)\)\}" target="_blank" rel="noopener">Open source/);
  assert.match(source,/data-ce-record-page/);assert.match(source,/p\.items\.map\(recordHTML\)/);
 });
+
+test('an explicit Summa question outside the held volume is a target conflict',()=>{
+ const target={work:{title:'Summa Theologica I-II, q. 1-70'}};
+ assert.equal(E.targetLocationConflict({loc:'1.2.q.81.art.3'},target),true);
+ assert.equal(E.targetLocationConflict({loc:'I-II q. 81, art. 3'},target),true);
+ assert.equal(E.targetLocationConflict({loc:'II-II q. 12'},target),true);
+});
+test('Summa range checks preserve valid boundary questions',()=>{
+ const target={work:{title:'Summa Theologica I-II, q. 1–70'}};
+ for(const loc of ['1.2.q.1.art.1','I-II q. 70','I-II q. 25, art. 3'])assert.equal(E.targetLocationConflict({loc},target),false);
+});
+test('ambiguous citations and unrelated title numbers never establish a target conflict',()=>{
+ for(const loc of ['1.2. of this question','third part of St. Thomas, qq. 27–71','q. 81','I-II q. 1-81'])assert.equal(E.targetLocationConflict({loc},{work:{title:'Summa Theologica I-II, q. 1-70'}}),false);
+ assert.equal(E.targetLocationConflict({loc:'I-II q. 81'},{work:{title:'Commentary, Vol. 1-70'}}),false);
+});
--- a/tools/prdl_reader_prototype/constellations.css
+++ b/tools/prdl_reader_prototype/constellations.css
@@ -74,7 +74,7 @@
 body.web-page .ce-next-list{max-height:min(55dvh,620px);overscroll-behavior:contain;margin-top:12px;scrollbar-gutter:stable}.ce-next-list:has(.ce-next-author[open]){max-height:none!important;overflow:visible}
 .ce-next-author{border-top:1px solid var(--border)}.ce-next-author:last-child{border-bottom:1px solid var(--border)}.ce-next-author>summary{display:flex;align-items:center;gap:12px;min-height:56px;list-style:none;padding:12px 0;cursor:pointer;font:16px/1.5 var(--body)}.ce-next-author>summary::-webkit-details-marker{display:none}.ce-next-author>summary::after{content:'+';color:var(--muted);flex:none}.ce-next-author[open]>summary::after{content:'−'}.ce-next-author>summary>span{flex:1;min-width:0;overflow-wrap:anywhere}.ce-next-author>summary small{font-size:12px;color:var(--muted);text-align:right}.ce-next-author>summary>span small{display:block;text-align:left}
 .ce-next-author[open]>summary{border-bottom:1px solid var(--border)}.ce-next-actions{display:flex;align-items:center;gap:8px 16px;flex-wrap:wrap;padding:14px 0}.ce-next-actions button{width:auto!important;border:1px solid var(--border)!important;border-radius:5px!important;padding:8px 12px!important;min-height:44px}
-body.web-page .ce-next-evidence .ce-pane{max-height:62dvh}.ce-next-evidence .ce-explanation{margin:4px 0 12px}.ce-next-evidence .ce-work>summary{flex-wrap:wrap}.ce-next-evidence .ce-work>summary>span{flex-basis:calc(100% - 48px)}.ce-next-evidence .ce-fold-body{padding-left:0}
+body.web-page .ce-next-evidence .ce-pane{max-height:62dvh}.ce-next-evidence .ce-explanation{margin:4px 0 12px}.ce-next-evidence .ce-fold-body{padding-left:0}
 body.web-page .ce-next-more{margin-top:14px}.ce-branch-history{border-top:1px solid var(--border)}.ce-branch-history>summary{min-height:48px;align-content:center;cursor:pointer;font-size:14px}
 body.web-page.popen label[for="map-density"]{display:none}
 @media(max-width:760px){body.web-page header.site{display:grid;grid-template-columns:minmax(0,1fr) auto auto auto;grid-template-rows:44px 48px;gap:0 8px;padding:6px 16px 8px}body.web-page header.site .brand{grid-column:1;grid-row:1;min-width:0}body.web-page header.site>.fr-explore{grid-column:4;grid-row:1;margin:0;font-size:13px}body.web-page header.site>.fr-explore>nav{display:grid;grid-template-columns:1fr 1fr;gap:0}body.web-page header.site>.fr-explore:not([open])>nav{display:none}body.web-page header.site #atlas-ask{grid-column:2;grid-row:1;margin:0}body.web-page header.site #web-theme{grid-column:3;grid-row:1;min-width:44px}body.web-page header.site>#mnav{grid-column:1/-1;grid-row:2;display:flex;justify-content:start;gap:20px;margin:0;min-width:0;width:100%}body.web-page header.site>#mnav>a{flex:none;font:14px var(--body);letter-spacing:0;text-transform:none}body.web-page #panel .ph{padding:16px 64px 14px 20px}body.web-page #panel .ph h2{font-size:25px}body.web-page #pbody{padding:14px 20px 30px}.ce-connections-heading h3{font-size:23px}.ce-next-author>summary{font-size:16px}.ce-next-author>summary>small{max-width:100px}.ce-journey-controls{gap:14px!important}}
@@ -107,5 +107,7 @@
 /* Keep the current branch and citation direction visible while learning through the graph. */
 .ce-journey-trail{display:flex;align-items:center;gap:4px 8px;flex-wrap:wrap;font:13px/1.45 var(--body);margin:0 0 6px;color:var(--muted)}.ce-journey-trail a{display:inline-flex;align-items:center;min-height:40px;color:var(--fg);text-decoration:underline;text-underline-offset:3px}.ce-journey-trail [aria-current]{font-weight:600;color:var(--fg)}
 .ce-direction-help{font:13px/1.5 var(--body);color:var(--muted);margin:0 0 10px}
-.ce-direction-switch{display:flex;gap:8px;margin-bottom:8px}.ce-journey .ce-direction-switch button{min-height:44px;padding:8px 14px;font-size:13px}.ce-direction-switch [aria-pressed="true"]{background:var(--fg);color:var(--bg)}
+.ce-direction-switch{display:flex;flex-wrap:wrap;gap:8px;margin-bottom:8px}.ce-journey .ce-direction-switch button{min-height:44px;padding:8px 14px;font-size:13px}.ce-direction-switch [aria-pressed="true"]{background:var(--fg);color:var(--bg)}
 @media(max-width:760px){.ce-journey-trail{font-size:12px;gap:3px 7px}.ce-direction-help{font-size:12px}.ce-journey-trail a{min-height:40px}.ce-direction-switch{margin-bottom:4px}}
+
+.ce-path-direction{font:13px/1.5 var(--body);margin:0 0 10px;color:var(--fg)}
--- a/tools/prdl_reader_prototype/journey-state.test.cjs
+++ b/tools/prdl_reader_prototype/journey-state.test.cjs
@@ -20,3 +20,13 @@
 });
 test('directed route keys survive URL round trips that omit redundant edge directions',()=>{assert.equal(E.journeyViewKey(['a','b'],'out','','',['a|b']),E.journeyViewKey(['a','b'],'out'));assert.equal(E.journeyViewKey(['a','b'],'in','','',['b|a']),E.journeyViewKey(['a','b'],'in'));});
 test('returning to a multi-step path reopens the actual evidence steps in citation direction',()=>{const steps=[{key:'b|a',citing:{a:'B'},target:{a:'A'}},{key:'c|b',citing:{a:'C'},target:{a:'B'}}],html=E.journeyStepFolds(steps,['b|a']);assert.match(html,/data-journey-step="0" open/);assert.doesNotMatch(html,/data-journey-step="1" open/);assert.match(html,/B cites A/);assert.match(html,/C cites B/);});
+
+test('a found mixed path and its URL round trip restore the same directed view',async()=>{
+ const html=fs.readFileSync(__dirname+'/web_shell.html','utf8'),start=html.indexOf('async function openJourney('),end=html.indexOf('/* ── doctrine flow',start),lookups=[];
+ const nodes=[{i:0,s:'a',a:'Earlier'},{i:1,s:'b',a:'Later'}];
+ const box={FRConnectionEvidence:E,NODES:nodes,EDGES:[[1,0,4]],BYS:{a:nodes[0],b:nodes[1]},__PSEQ:0,stashJourney(){},journeyViews:{get(key){lookups.push(key);}},AbortController,history:{pushState(){throw Error('stop before DOM');}},journeyURL(){return '/web';}};
+ vm.createContext(box);vm.runInContext(html.slice(start,end),box);
+ await assert.rejects(box.openJourney(['a','b'],'either'),/stop before DOM/);
+ await assert.rejects(box.openJourney(['a','b'],'either',{via:['b|a']}),/stop before DOM/);
+ assert.equal(lookups.length,2);assert.equal(lookups[0],lookups[1]);assert.equal(JSON.parse(lookups[0])[4][0],'b|a');
+});
--- a/tools/prdl_reader_prototype/web_shell.html
+++ b/tools/prdl_reader_prototype/web_shell.html
@@ -868,7 +868,6 @@
 function rememberJourney(chain,direction,work,targetWork,via=[]){if(!chain.length)return;const url=journeyURL(chain,direction,work,targetWork,'',via);if(!journeyBranches.some(b=>b.url===url))journeyBranches.unshift({url,label:chain.map(s=>BYS[s]?.a||s).join(' › ')+' · '+(direction==='in'?'Reception':direction==='out'?'Sources':'Both directions')});if(journeyBranches.length>12)journeyBranches.length=12;}
 async function openJourney(chain,direction='in',options={}){
  stashJourney();const token=++__PSEQ;direction=['in','out','either'].includes(direction)?direction:'in';const work=options.work||'',targetWork=options.targetWork||'',via=options.via||[];
- const viewKey=FRConnectionEvidence.journeyViewKey(chain,direction,work,targetWork,via),resume=journeyViews.get(viewKey),evidenceViews=new Map(Object.entries(resume?.evidence||{})),controllers=new Map(),readyViews=new Set(),viewEvents=new AbortController();let restorePending=!!resume;
  const nodes=chain.map(s=>BYS[s]);
  if(!nodes.length||nodes.some(n=>!n)){openPanel('Choose an author','This author is not in the published citation graph.');pbody.innerHTML='<p class="ce-note">Return to the index to find an author.</p><button class="pbtn" id="journey-return">Browse authors</button>';$('#journey-return').onclick=()=>$('#px').click();return;}
  const repeated=chain.findIndex((s,i)=>chain.indexOf(s)!==i);
@@ -880,6 +879,7 @@
    steps.push({citing:NODES[edge[0]],target:NODES[edge[1]],count:edge[2],key:NODES[edge[0]].s+'|'+NODES[edge[1]].s});
  }
  const stepKeys=steps.map(s=>s.key),current=nodes.at(-1),mode=direction==='in'?'Follow reception':direction==='out'?'Follow sources':'Follow connections';
+ const viewKey=FRConnectionEvidence.journeyViewKey(chain,direction,work,targetWork,stepKeys),resume=journeyViews.get(viewKey),evidenceViews=new Map(Object.entries(resume?.evidence||{})),controllers=new Map(),readyViews=new Set(),viewEvents=new AbortController();let restorePending=!!resume;
  if(options.push!==false)history.pushState(null,'',journeyURL(chain,direction,work,targetWork,options.reference||'',stepKeys));
  PATHV=nodes.map(n=>n.i);window.__CHAIN=chain;window.__JDIR=direction;FOCUS=null;
  const connections=FRConnectionEvidence.journeyConnections(NODES,EDGES,current.i,direction),around=connections.slice(0,80).map(r=>r.node.i);
@@ -888,7 +888,7 @@
  $('#map-summary').textContent=connections.length.toLocaleString()+' recorded '+(direction==='out'?'source':direction==='in'?'reception':'author')+' connections'+(connections.length>80?' · strongest 80 shown on the map':'');draw();
  const authorOptions=NODES.slice().sort((a,b)=>a.a.localeCompare(b.a)).map(n=>`<option value="${esc(n.s)}"${n.s===current.s?' selected':''}>${esc(n.a)}${n.sh?' · '+esc(SHN[n.sh]||''):''}</option>`).join('');
  const heading=direction==='out'?'Cited authors':direction==='in'?'Citing authors':'Connected authors';
- pbody.innerHTML=`<div class="ce-journey"><nav class="ce-journey-trail" aria-label="Current research path">${nodes.map((n,i)=>i===nodes.length-1?`<span aria-current="step">${esc(n.a)}</span>`:`<a href="${journeyURL(chain.slice(0,i+1),direction,work,targetWork,'',via.slice(0,i))}">${esc(n.a)}</a>`).join('<span aria-hidden="true">›</span>')}</nav><p class="ce-direction-help">${direction==='out'?'Recorded sources in works indexed under '+esc(current.a)+'.':direction==='in'?'Recorded references to '+esc(current.a)+' in other authors’ works.':'Recorded citations in either direction; each connection names who cites whom.'}</p><div class="ce-direction-switch" aria-label="Citation direction"><button data-journey-direction="out" aria-pressed="${direction==='out'}">Sources</button><button data-journey-direction="in" aria-pressed="${direction==='in'}">Reception</button></div><details class="ce-journey-options"><summary>Change author or direction</summary><div class="ce-journey-controls"><label>Choose author<select id="journey-author">${authorOptions}</select></label><label>Follow<select id="journey-direction"><option value="out">Sources this author cites</option><option value="in">Authors who cite this author</option><option value="either">Both directions</option></select></label></div></details>${steps.length?`<details class="ce-path-history"><summary>Path so far · ${nodes.length} authors</summary><ol class="ce-chain">${nodes.map((n,i)=>`<li><a href="${journeyURL(chain.slice(0,i+1),direction,work,targetWork,'',via.slice(0,i))}">${esc(n.a)}</a><small>${n.yok?'c. '+n.y+' · ':''}${esc(SHN[n.sh]||'Indexed author')}</small></li>`).join('')}</ol><div class="ce-path-steps">${FRConnectionEvidence.journeyStepFolds(steps,resume?.openSteps||[])}</div><button class="ce-save-path" disabled>Save selected journey</button><p class="ce-journey-feedback" id="journey-selection" role="status">Select passages from the path to save them together.</p></details>`:''}<div class="ce-connections-heading"><h3>${esc(heading)}</h3><a href="/web#a=${encodeURIComponent(current.s)}">Author overview</a></div><label><span class="sr-only">Find a cited author</span><input type="search" id="journey-query" placeholder="Filter these authors"></label><p class="ce-journey-feedback" id="journey-next-count" role="status"></p><div class="ce-next-list"></div><nav class="ce-pager ce-journey-pager" aria-label="Connection pages"><button class="ce-next-previous" hidden>Previous</button><button class="ce-next-more" hidden>Next authors</button></nav><details class="ce-about"><summary>About these connections</summary><p class="ce-note">Open an author to read recorded references, grouped by their citing work. References are indexed under a work’s author; correspondence and editorial material can include other voices. Inspect the source before attributing a statement. Dates describe authors, not publication dates.</p></details><details class="ce-branch-history"><summary>Recently explored branches</summary><div></div></details></div>`;
+ pbody.innerHTML=`<div class="ce-journey"><nav class="ce-journey-trail" aria-label="Current research path">${nodes.map((n,i)=>i===nodes.length-1?`<span aria-current="step">${esc(n.a)}</span>`:`<a href="${journeyURL(chain.slice(0,i+1),direction,work,targetWork,'',stepKeys.slice(0,i))}">${esc(n.a)}</a>`).join('<span aria-hidden="true">›</span>')}</nav><p class="ce-direction-help">${direction==='out'?'Recorded sources in works indexed under '+esc(current.a)+'.':direction==='in'?'Recorded references to '+esc(current.a)+' in other authors’ works.':'Recorded citations in either direction; each connection names who cites whom.'}</p>${steps.length?`<p class="ce-path-direction">Latest step: ${esc(steps.at(-1).citing.a)} cites ${esc(steps.at(-1).target.a)}.</p>`:''}<div class="ce-direction-switch" aria-label="Citation direction"><button data-journey-direction="out" aria-pressed="${direction==='out'}">Sources</button><button data-journey-direction="in" aria-pressed="${direction==='in'}">Reception</button><button data-journey-direction="either" aria-pressed="${direction==='either'}">Both directions</button></div><details class="ce-journey-options"><summary>Change author or direction</summary><div class="ce-journey-controls"><label>Choose author<select id="journey-author">${authorOptions}</select></label><label>Follow<select id="journey-direction"><option value="out">Sources this author cites</option><option value="in">Authors who cite this author</option><option value="either">Both directions</option></select></label></div></details>${steps.length?`<details class="ce-path-history"><summary>Path so far · ${nodes.length} authors</summary><ol class="ce-chain">${nodes.map((n,i)=>`<li><a href="${journeyURL(chain.slice(0,i+1),direction,work,targetWork,'',stepKeys.slice(0,i))}">${esc(n.a)}</a><small>${n.yok?'c. '+n.y+' · ':''}${esc(SHN[n.sh]||'Indexed author')}</small></li>`).join('')}</ol><div class="ce-path-steps">${FRConnectionEvidence.journeyStepFolds(steps,resume?.openSteps||[])}</div><button class="ce-save-path" disabled>Save selected journey</button><p class="ce-journey-feedback" id="journey-selection" role="status">Select passages from the path to save them together.</p></details>`:''}<div class="ce-connections-heading"><h3>${esc(heading)}</h3><a href="/web#a=${encodeURIComponent(current.s)}">Author overview</a></div><label><span class="sr-only">Find a cited author</span><input type="search" id="journey-query" placeholder="Filter these authors"></label><p class="ce-journey-feedback" id="journey-next-count" role="status"></p><div class="ce-next-list"></div><nav class="ce-pager ce-journey-pager" aria-label="Connection pages"><button class="ce-next-previous" hidden>Previous</button><button class="ce-next-more" hidden>Next authors</button></nav><details class="ce-about"><summary>About these connections</summary><p class="ce-note">Open an author to read recorded references, grouped by their citing work. References are indexed under a work’s author; correspondence and editorial material can include other voices. Inspect the source before attributing a statement. Dates describe authors, not publication dates.</p></details><details class="ce-branch-history"><summary>Recently explored branches</summary><div></div></details></div>`;
  $('#journey-direction').value=direction;$('#journey-query').value=resume?.query||'';if(resume?.pathOpen&&$('.ce-path-history'))$('.ce-path-history').open=true;
  if(direction!=='out')$('#journey-query').previousElementSibling.textContent='Find a connected author';
  $('.ce-branch-history div').innerHTML=journeyBranches.filter(b=>b.url!==journeyURL(chain,direction,work,targetWork,'',stepKeys)).map(b=>`<a href="${esc(b.url)}">${esc(b.label)}</a>`).join('')||'<p class="ce-note">Branches you explore in this session appear here.</p>';
