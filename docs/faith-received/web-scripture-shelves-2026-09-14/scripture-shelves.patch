--- a/tools/prdl_reader_prototype/connection-evidence.js
+++ b/tools/prdl_reader_prototype/connection-evidence.js
@@ -246,7 +246,9 @@
     let books;try{books=(await json('/v1/bible/all/books.json')).books||[];}catch(_){overlap.innerHTML='<p class="ce-note">Scripture navigation could not load. Close this explanation and select the connection again to retry.</p>';return ()=>{alive=false;};}
     if(!current())return;
     const shared=sharedChapters(leftRows,rightRows,books);
-    overlap.innerHTML=`<h6>${count(shared.length)} shared chapters in the supplied profiles</h6><p class="ce-note">These exported profiles contain selected chapters. Open a chapter to check which indexed verses and source pages are present for both entries.</p>${shared.map((r,i)=>`<details class="ce-chapter" data-ce-chapter="${i}"><summary><span>${esc(r.label)}</span><small>${count(r.left)} / ${count(r.right)} citations</small></summary><p class="ce-note">Counts correspond to ${esc(name(left))} / ${esc(name(right))}.</p><div class="ce-chapter-evidence"></div></details>`).join('')||'<p class="ce-note">No common chapter appears in these exported profiles. The map was calculated from a larger citation index.</p>'}`;
+    let chapterPage=0;function renderChapters(){const win=pageWindow(shared,chapterPage,8);chapterPage=win.page;
+    overlap.innerHTML=`<h6>${count(shared.length)} shared chapters in the supplied profiles</h6><p class="ce-note">These exported profiles contain selected chapters. Open a chapter to check which indexed verses and source pages are present for both entries.</p>${shared.map((r,i)=>`<details class="ce-chapter" data-ce-chapter="${i}"><summary><span>${esc(r.label)}</span><small>${count(r.left)} / ${count(r.right)} citations</small></summary><p class="ce-note">Counts correspond to ${esc(name(left))} / ${esc(name(right))}.</p><div class="ce-chapter-evidence"></div></details>`).slice(win.start,win.end).join('')||'<p class="ce-note">No common chapter appears in these exported profiles. The map was calculated from a larger citation index.</p>'}${win.pages>1?`<nav class="ce-pager" aria-label="Shared chapter pages"><button data-chapter-prev${!win.page?' disabled':''}>Previous</button><span>Chapters ${win.start+1}–${win.end} of ${shared.length}</span><button data-chapter-next${win.page+1===win.pages?' disabled':''}>Next chapters</button></nav>`:''}`;
+    overlap.querySelector('[data-chapter-prev]')?.addEventListener('click',()=>{chapterPage--;renderChapters();});overlap.querySelector('[data-chapter-next]')?.addEventListener('click',()=>{chapterPage++;renderChapters();});
     overlap.querySelectorAll('[data-ce-chapter]').forEach(details=>{
       let loaded=false,pending=false;
       const load=async()=>{if(!details.open||loaded||pending)return;pending=true;const r=shared[+details.dataset.ceChapter],box=details.querySelector('.ce-chapter-evidence');box.innerHTML='<p class="ce-note" role="status">Finding verse references and source passages…</p>';
@@ -256,7 +258,7 @@
           bind();
         }catch(_){box.innerHTML='<p class="ce-note">This chapter could not load. Your connection remains open.</p><button class="ce-retry">Retry chapter</button>';box.querySelector('button').onclick=load;}finally{pending=false;}
       };details.addEventListener('toggle',load);
-    });return ()=>{alive=false;};
+    });}renderChapters();return ()=>{alive=false;};
   }
   const api={targetLocationConflict,journeyStepFolds,citationViewState,journeyViewKey,pageWindow,citationGroups,esc,fold,source,withHl,readerURL,safeURL,chapterReference,sharedChapters,sharedPages,verseOverlap,citationDirection,directedPath,journeyEdge,journeyConnections,filterCitations,pageContext,resolutionLabel,resolvedTargets,json,preview,bindPreviews,researchNote,save,mountCitation,mountComparison};
   if(typeof module==='object'&&module.exports)module.exports=api;else root.FRConnectionEvidence=api;
--- a/tools/prdl_reader_prototype/constellations.css
+++ b/tools/prdl_reader_prototype/constellations.css
@@ -111,3 +111,10 @@
 @media(max-width:760px){.ce-journey-trail{font-size:12px;gap:3px 7px}.ce-direction-help{font-size:12px}.ce-journey-trail a{min-height:40px}.ce-direction-switch{margin-bottom:4px}}
 
 .ce-path-direction{font:13px/1.5 var(--body);margin:0 0 10px;color:var(--fg)}
+
+/* Scripture is one verse at a time, with complete available source groups. */
+.web-verse-nav{display:grid;grid-template-columns:auto minmax(0,1fr) auto;align-items:end;gap:10px;margin:12px 0}.web-verse-nav label,.web-verse-find{display:grid;gap:6px;font:13px var(--body);color:var(--muted)}.web-verse-nav select{width:100%}.web-verse-nav button,.web-verse-nav select{min-height:44px}
+.web-selected-verse{padding:14px 0;margin:8px 0 16px;border-block:1px solid var(--border)}body.web-page #pbody .web-selected-verse h3{font:21px/1.25 var(--display);margin:0 0 8px}.web-selected-verse>p{font:16px/1.65 var(--body);margin:0 0 8px}.web-selected-verse>a{display:inline-flex;align-items:center;min-height:44px;font-size:13px}.web-verse-find input{width:100%;margin-bottom:4px}
+.web-verse-author{border-top:1px solid var(--border)}.connection-evidence .web-verse-author>summary{display:grid;grid-template-columns:minmax(0,1fr) 20px;gap:2px 12px;padding:12px 0;min-height:60px;list-style:none}.web-verse-author>summary>span{font-weight:600;grid-column:1;grid-row:1}.web-verse-author>summary>small{grid-column:1;grid-row:2;font:12px/1.4 var(--body);color:var(--muted)}.web-verse-author>summary::after{content:'+';grid-column:2;grid-row:1/3;align-self:center}.web-verse-author[open]>summary::after{content:'−'}.web-verse-author>summary::-webkit-details-marker{display:none}.web-author-works{padding:0 0 12px 12px;border-left:1px solid var(--border);margin-bottom:8px}
+body.web-page .web-work-sources .web-verse-source{display:block;padding:8px 0;border-top:1px solid var(--border)}.web-source-actions{display:flex;gap:8px 14px;align-items:center;flex-wrap:wrap}.web-source-actions>a{display:inline-flex;align-items:center;min-height:44px;font-size:13px}.web-scripture-evidence .ce-preview{margin:2px 0 10px}.web-scripture-evidence .ce-pager{margin:8px 0}.web-scripture-evidence .scripture-status{font-size:12px;line-height:1.5;margin:6px 0 12px}.web-author-works .ce-work>summary{font-size:14px}.web-source-actions button{font-size:13px}
+@media(max-width:760px){body.web-page.shelf-comparison #pbody .sat-head h3{font-size:24px}.shelf-map-controls{flex-wrap:wrap}.shelf-map-controls label{flex-basis:150px}.shelf-map-controls select{font-size:16px}body.web-page.shelf-comparison:has(.sat-selected) .shelf-map-controls{display:none}.web-selected-verse>p{font-size:16px}.web-author-works{padding-left:8px}}
--- a/tools/prdl_reader_prototype/scripture-tools.js
+++ b/tools/prdl_reader_prototype/scripture-tools.js
@@ -48,6 +48,55 @@
     const row=w=>'<div class="annotation-volume"><div><strong>'+escape(w.title)+'</strong>'+(w.volume?'<span>'+escape(w.volume)+'</span>':'')+'<small>'+escape(w.hit?w.hit.heading:w.available?'No matching book section in the available contents.':'The table of contents is unavailable. Browse the volume to find a passage.')+'</small></div><a href="'+readerURL(w.slug,w.hit?.page)+'" target="_blank" rel="noopener">'+(w.hit?.kind==='chapter'?'Read chapter':w.hit?.kind==='citation'?'Read cited passage':w.hit?'Read book section':'Browse volume')+'</a></div>';
     container.innerHTML=(matched.length?matched.map(row).join(''):'<p class="scripture-status">A matching section could not be located. You can browse the volumes below.</p>')+(rest.length?'<details class="annotation-other"'+(!matched.length?' open':'')+'><summary>Browse '+rest.length+' '+(matched.length?'other ':'')+'volume'+(rest.length===1?'':'s')+'</summary>'+rest.map(row).join('')+'</details>':'');
   }
-  const api={escape,readerURL,bibleURL,roman,bookMatch,locate,family,catalogue,volumes,renderVolumes};
+
+  const foldName=value=>String(value||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim();
+  function verseGroups(rows,works=new Map()){
+    const authors=new Map();
+    for(const row of rows||[]){const author=String(row.a||'Unattributed'),slug=String(row.w||''),meta=works.get(slug)||{};
+      if(!authors.has(author))authors.set(author,{author,records:0,works:new Map()});const a=authors.get(author);a.records++;
+      if(!a.works.has(slug))a.works.set(slug,{slug,title:meta.title_en||meta.title||row.wt||slug||'Work not identified',volume:meta.volume||'',records:0,pages:new Map()});
+      const w=a.works.get(slug),page=row.p==null?'':String(row.p);w.records++;w.pages.set(page,(w.pages.get(page)||0)+1);
+    }
+    return [...authors.values()].map(a=>({...a,works:[...a.works.values()].map(w=>({...w,pages:[...w.pages].sort((a,b)=>a[0].localeCompare(b[0],undefined,{numeric:true}))}))})).sort((a,b)=>b.records-a.records||a.author.localeCompare(b.author));
+  }
+  const webVerseURL=(book,ch,state={})=>'/web#v='+encodeURIComponent(book)+'/'+ch+'?'+new URLSearchParams({verse:state.verse||1,...state.author?{author:state.author}:{},...state.query?{q:state.query}:{},...state.page?{page:state.page}:{}});
+  async function renderWebVerse(host,chapter,options={}){
+    const E=root.FRConnectionEvidence,verses=chapter.verses||[],book=options.book,ch=options.ch;
+    let verse=verses.find(v=>String(v.v)===String(options.verse))||verses[0],author=options.author||'',query=options.query||'',page=+options.page||0;
+    let works=new Map();try{works=(await root.FRResearchData.corpus()).works;}catch(_){}if(!host.isConnected)return;
+    if(!verse){host.innerHTML='<p class="scripture-status">No verses are available for this chapter.</p>';return;}
+    const state=()=>({verse:verse.v,author,query,page}),remember=()=>options.onChange?.(state());
+    host.classList.add('web-scripture-evidence','connection-evidence');
+    host.innerHTML=`<div class="web-verse-nav"><button type="button" data-verse-prev aria-label="Previous verse">Previous</button><label>Verse<select data-verse>${verses.map(v=>`<option value="${v.v}">${v.v}</option>`).join('')}</select></label><button type="button" data-verse-next aria-label="Next verse">Next</button></div><article class="web-selected-verse"></article><label class="web-verse-find">Find an author or work<input type="search" data-verse-query placeholder="Luther, Augustine, or a work title"></label><p class="scripture-status" data-verse-count role="status"></p><div class="web-verse-authors"></div><nav class="ce-pager" aria-label="Verse author pages"><button data-authors-prev>Previous</button><span data-author-page></span><button data-authors-next>Next authors</button></nav>`;
+    const find=s=>host.querySelector(s);find('[data-verse-query]').value=query;
+    function renderAuthors(){
+      const groups=verseGroups(verse.rows,works),q=foldName(query),hits=groups.filter(a=>!q||foldName([a.author,...a.works.map(w=>w.title+' '+w.volume)].join(' ')).includes(q));
+      const win=E.pageWindow(hits,page,12);page=win.page;
+      find('[data-verse-count]').textContent=hits.length+(hits.length===1?' author':' authors')+(q?' matching your search':'')+' · '+(q?hits.reduce((n,a)=>n+a.records,0).toLocaleString()+' of ':'')+(verse.rows||[]).length.toLocaleString()+' available indexed records. Multiple records can refer to the same source location.';
+      find('[data-author-page]').textContent='Page '+(win.page+1)+' of '+win.pages;find('[data-authors-prev]').disabled=win.page===0;find('[data-authors-next]').disabled=win.page+1===win.pages;
+      find('.web-verse-authors').innerHTML=win.items.map((a,i)=>`<details class="web-verse-author" data-verse-author="${i}"${a.author===author?' open':''}><summary><span>${escape(a.author)}</span><small>${a.works.length} ${a.works.length===1?'work':'works'} · ${a.records} records</small></summary><div class="web-author-works"></div></details>`).join('')||'<p class="scripture-status">No author or work matches. Clear the search to see the available sources.</p>';
+      host.querySelectorAll('[data-verse-author]').forEach(d=>{const a=win.items[+d.dataset.verseAuthor];let loaded=false;
+        const open=()=>{if(!d.open){if(author===a.author){author='';remember();}return;}author=a.author;host.querySelectorAll('.web-verse-author[open]').forEach(other=>{if(other!==d)other.open=false;});remember();if(loaded)return;loaded=true;
+          const target=d.querySelector('.web-author-works');let workPage=0;const recordPages=new Map();
+          function workList(){const ordered=root.FRResearch?.orderedWorks?root.FRResearch.orderedWorks(a.works,w=>({...works.get(w.slug),w:w.slug})):a.works;const wwin=E.pageWindow(ordered,workPage,6);workPage=wwin.page;
+            target.innerHTML=(wwin.pages>1?`<nav class="ce-pager" aria-label="Source work pages"><button data-work-prev${!workPage?' disabled':''}>Previous</button><span>Works ${wwin.start+1}–${wwin.end} of ${a.works.length}</span><button data-work-next${workPage+1===wwin.pages?' disabled':''}>Next works</button></nav>`:'')+wwin.items.map((w,i)=>`<details class="ce-work" data-verse-work="${i}"${i===0?' open':''}><summary><span><strong>${escape(w.title)}${w.volume?' · '+escape(w.volume):''}</strong></span><small class="ce-n">${w.pages.length} source ${w.pages.length===1?'location':'locations'}</small></summary><div class="web-work-sources"></div></details>`).join('');
+            target.querySelector('[data-work-prev]')?.addEventListener('click',()=>{workPage--;workList();});target.querySelector('[data-work-next]')?.addEventListener('click',()=>{workPage++;workList();});
+            target.querySelectorAll('[data-verse-work]').forEach(wd=>{const w=wwin.items[+wd.dataset.verseWork],body=wd.querySelector('.web-work-sources');
+              const draw=()=>{const p=E.pageWindow(w.pages,recordPages.get(w.slug)||0,5);recordPages.set(w.slug,p.page);
+                body.innerHTML=(p.pages>1?`<nav class="ce-pager" aria-label="Source location pages"><button data-source-prev${!p.page?' disabled':''}>Previous</button><span>${p.start+1}–${p.end} of ${w.pages.length}</span><button data-source-next${p.page+1===p.pages?' disabled':''}>Next</button></nav>`:'')+p.items.map(([loc,n])=>{const url=w.slug?readerURL(w.slug,loc):'';return `<article class="web-verse-source"><p class="scripture-status">${loc?'Source location '+escape(loc):'Location not supplied'}${n>1?' · '+n+' indexed records':''}</p>${url?`<div class="web-source-actions"><a href="${escape(url)}" target="_blank" rel="noopener">Open source</a>${options.saveBtn?.(w.slug,loc,w.title,a.author,chapter.book+' '+ch+':'+verse.v)||''}</div>${E.preview(url)}`:'<p>The source work has not been identified.</p>'}</article>`;}).join('');E.bindPreviews(body);
+                body.querySelector('[data-source-prev]')?.addEventListener('click',()=>{recordPages.set(w.slug,p.page-1);draw();});body.querySelector('[data-source-next]')?.addEventListener('click',()=>{recordPages.set(w.slug,p.page+1);draw();});};draw();
+            });
+          }workList();
+        };d.addEventListener('toggle',open);if(d.open)open();
+      });remember();
+    }
+    function showVerse(){const index=verses.indexOf(verse);find('[data-verse]').value=verse.v;find('[data-verse-prev]').disabled=index===0;find('[data-verse-next]').disabled=index===verses.length-1;
+      find('.web-selected-verse').innerHTML=`<h3>${escape(chapter.book)} ${ch}:${verse.v}</h3><p>${escape(verse.t||'Verse text is unavailable.')}</p><a href="${bibleURL(book,ch,verse.v)}" target="_blank" rel="noopener">Explore this verse in Scripture</a>`;renderAuthors();}
+    function changeVerse(v){verse=v;page=0;author='';showVerse();options.onVerse?.(state());}
+    find('[data-verse]').onchange=e=>changeVerse(verses.find(v=>String(v.v)===e.target.value)||verses[0]);find('[data-verse-prev]').onclick=()=>changeVerse(verses[Math.max(0,verses.indexOf(verse)-1)]);find('[data-verse-next]').onclick=()=>changeVerse(verses[Math.min(verses.length-1,verses.indexOf(verse)+1)]);
+    find('[data-verse-query]').oninput=e=>{query=e.target.value;page=0;renderAuthors();};find('[data-authors-prev]').onclick=()=>{page--;renderAuthors();find('.web-verse-authors').scrollIntoView({block:'start'});};find('[data-authors-next]').onclick=()=>{page++;renderAuthors();find('.web-verse-authors').scrollIntoView({block:'start'});};showVerse();return {state};
+  }
+
+  const api={verseGroups,webVerseURL,renderWebVerse,escape,readerURL,bibleURL,roman,bookMatch,locate,family,catalogue,volumes,renderVolumes};
   if(typeof module==='object'&&module.exports)module.exports=api;else root.FRScripture=api;
 })(typeof window==='undefined'?globalThis:window);
--- a/tools/prdl_reader_prototype/scripture-tools.test.cjs
+++ b/tools/prdl_reader_prototype/scripture-tools.test.cjs
@@ -51,3 +51,16 @@
  assert.equal(rows[0].slug,'eebo-two');assert.equal(rows[0].hit.kind,'citation');assert.equal(rows[0].hit.page,'52:0183A');
  assert.equal(rows.find(r=>r.slug==='eebo-one').hit,null);
 });
+
+test('verse evidence keeps every record and every work behind an author',()=>{
+ const groups=s.verseGroups([{a:'Luther',w:'vol20',p:318},{a:'Luther',w:'vol20',p:318},{a:'Luther',w:'vol21',p:'52:0183A'},{a:'Baxter',w:'baxter',p:0}],new Map([['vol20',{title:'Writings',volume:'20'}]]));
+ assert.equal(groups.length,2);assert.equal(groups[0].records,3);assert.equal(groups[0].works.length,2);assert.equal(groups[0].works[0].pages[0][1],2);assert.equal(groups[0].works[1].pages[0][0],'52:0183A');assert.equal(groups[1].works[0].pages[0][0],'0');
+});
+test('verse grouping keeps witnesses separate and sorts opaque source locations',()=>{
+ const groups=s.verseGroups([{a:'A',w:'first',p:'52:0183B'},{a:'A',w:'second',p:'52:0183A'},{a:'A',w:'first',p:'52:0183A'}]);
+ assert.equal(groups[0].works.length,2);assert.equal(groups[0].works[0].pages.map(p=>p[0]).join(','),'52:0183A,52:0183B');
+});
+test('Web verse URLs preserve the selected verse and accented author search',()=>{
+ const url=s.webVerseURL('romans',8,{verse:11,author:'Gabriel Vásquez',query:'grace & mercy',page:2});
+ const query=new URLSearchParams(url.split('?')[1]);assert.equal(query.get('verse'),'11');assert.equal(query.get('author'),'Gabriel Vásquez');assert.equal(query.get('q'),'grace & mercy');assert.equal(query.get('page'),'2');
+});
--- a/tools/prdl_reader_prototype/shelf-constellations.css
+++ b/tools/prdl_reader_prototype/shelf-constellations.css
@@ -12,3 +12,7 @@
 .sat-neighbour{display:grid;grid-template-columns:minmax(0,1fr) auto;align-items:center;gap:12px;border-bottom:1px solid var(--border)}.shelf-atlas .sat-neighbours .sat-neighbour button{border-bottom:0}.shelf-atlas .sat-neighbours .sat-neighbour button[data-entry]{width:auto;min-width:72px;font-size:12px;text-decoration:underline;text-underline-offset:4px}.sat-neighbour button>span>small{display:block;font-size:12px;white-space:normal;margin-top:5px;text-decoration:underline;text-underline-offset:4px}.sat-connection-evidence:not(:empty){margin:24px 0;padding:4px 0 24px;border-block:1px solid var(--border)}.shelf-atlas .sat-connection-evidence .ce-pick{display:flex;flex-direction:row}.sat-connection-evidence .ce-preview{overflow:visible}.sat-connection-evidence h5:focus{outline:none}
 
 @media(max-width:760px){.shelf-atlas{padding:12px 0}.sat-head h3,.sat-detail h4{font-size:24px;line-height:1.2;margin-bottom:8px}.sat-head>p{font-size:14px;line-height:1.5}.sat-method{margin:8px 0 14px}.sat-index,.sat-detail{padding-block:12px}.shelf-atlas .sat-entry{padding:10px 4px;min-height:52px}.sat-detail h5{font-size:20px;margin:20px 0 8px}.sat-sources article{padding:12px 0}.sat-sources article>p:not(.sat-note){font-size:16px;line-height:1.6;margin:8px 0}.shelf-atlas .sat-back{margin-bottom:12px}.sat-book>summary{font-size:18px;min-height:48px}}
+
+/* Choose an entry, then compare profiles or read its sources. */
+.shelf-atlas .sat-back{display:inline-flex;align-items:center;margin:0 0 12px}.sat-detail-tabs{display:flex;gap:8px;margin:14px 0 8px;border-bottom:1px solid var(--border);padding-bottom:10px}.shelf-atlas .sat-detail-tabs button[aria-pressed="true"]{background:var(--fg);color:var(--bg);border-color:var(--fg)}.sat-detail [data-sat-panel]>h5{margin-top:16px}.sat-comparison-back{margin:12px 0}.sat-connection-evidence:not(:empty){margin:12px 0;padding:8px 0}.sat-connection-evidence h5{font-size:22px}.sat-index>.ce-pager{font-size:12px}.sat-index>.ce-pager button{font-size:12px;padding:8px}.sat-entry strong::after{content:'›';float:right;color:var(--muted);padding-left:8px}.sat-detail .sat-sources article{padding:12px 0}.sat-neighbours{max-height:none;overflow:visible}.sat-neighbour button span{min-width:0;overflow-wrap:anywhere}.sat-detail [data-sat-panel="sources"] .sat-note{margin:6px 0}
+@media(max-width:760px){.sat-selected>.sat-head{display:none}.sat-detail-tabs{position:sticky;top:0;z-index:2;background:var(--card)}.sat-detail-tabs button{flex:1}.sat-layout .sat-detail{padding-top:10px!important}.sat-detail h4{font-size:24px}.sat-neighbour{gap:8px}.shelf-atlas .sat-neighbours .sat-neighbour button{padding:10px 0}.sat-index{padding-top:10px}.sat-index>.ce-pager button{font-size:13px}}
--- a/tools/prdl_reader_prototype/shelf-constellations.js
+++ b/tools/prdl_reader_prototype/shelf-constellations.js
@@ -10,26 +10,29 @@
  const doctrine=ctx.kind==='doctrines'||ctx.kind==='topics',kind=ctx.kind||K.f||'authors',nodes=data.nodes||[],edges=data.edges||[],adj=new Map();
  edges.forEach(([a,b,w])=>{if(!nodes[a]||!nodes[b])return;if(!adj.has(a))adj.set(a,[]);if(!adj.has(b))adj.set(b,[]);adj.get(a).push({i:b,w});adj.get(b).push({i:a,w});});for(const rows of adj.values())rows.sort((a,b)=>b.w-a.w);
  const rawLabel=n=>doctrine&&window.FRResearch.isRawTopic(K.name(n)),rawCount=nodes.filter(rawLabel).length;
- const unit=doctrine?'index weight':K.unit||'citations',sub=n=>n.sub?en(n.sub):number(n.n)+' '+unit;
- let limit=30,selected=-1,sequence=0,rowsPromise=null;
+ const unit=doctrine?'index weight':K.unit||'citations',sub=n=>n.sub?en(n.sub).replace(/\bmined pages\b/g,'indexed pages'):number(n.n)+' '+unit;
+ let page=+ctx.page||0,selected=-1,sequence=0,rowsPromise=null;const pageSize=12,windowOf=(rows,p,size)=>window.FRConnectionEvidence.pageWindow(rows,p,size);
  host.classList.add('shelf-atlas');
- host.innerHTML=`<div class="sat-head"><h3>${doctrine?(data.relation?'Topics found together':'Topics in the patrology index'):kind==='works'?'Connections between works':'Connections between authors'}</h3><p>${doctrine?(data.relation?'Explore subjects with similar patterns of recorded neighbouring topics.':'Explore the subject entries and supplied links in the published patrology index.'):'Explore similarities in the Scripture references recorded for these '+(kind==='works'?'works.':'authors.')}</p><p class="sat-coverage">${number(nodes.length)} mapped ${kind==='doctrines'?'topics':kind}${data.shelf?' · '+escape(data.shelf):''}${data.version?' · Index dated '+escape(data.version):''}${data.capped?' · '+number(data.capped)+' works excluded by the published map’s cap':''}</p><details class="sat-method"><summary>What these connections mean</summary><p>${!data.relation?'This historical index links published entries. Its export does not declare how each weight was calculated. Open Why connected to inspect the supplied endpoint evidence.':doctrine?'Edges compare which other topics occur alongside each subject. Similar profiles suggest places to investigate; they do not establish a shared conclusion.':'Edges represent the published similarity of Scripture citation profiles. Similarity does not show that these authors read one another or agree.'} The diagram displays up to six recorded neighbours; the list includes every edge available for the selected entry. Counts and samples cover this export, not necessarily the full shelf.</p></details></div><div class="sat-layout"><section class="sat-index" aria-label="Constellation index"><label>Find ${kind==='works'?'a work':doctrine?'a topic':'an author'}<input class="sat-find" type="search" placeholder="Search this constellation"></label><label>Order entries<select class="sat-sort"><option value="weight">${doctrine?'Highest index weight':'Most recorded citations'}</option><option value="name">Name A–Z</option><option value="links">Most connections</option></select></label>${rawCount?`<label class="sat-raw"><input class="sat-raw-toggle" type="checkbox">Include ${rawCount} raw index labels</label><p class="sat-note">Raw labels contain extraction notes and need editorial review.</p>`:''}<p class="sat-count" role="status"></p><div class="sat-list"></div><button class="sat-more">Show more entries</button></section><section class="sat-detail" aria-label="Selected entry"><div class="sat-empty"><h4>Follow a connection</h4><p>Choose an entry to see its recorded neighbours and read the passages behind it.</p><a href="/web">Explore the citation atlas</a></div></section></div>`;
- const find=s=>host.querySelector(s),drawList=()=>{const q=find('.sat-find').value.trim().toLowerCase(),order=find('.sat-sort').value,hits=nodes.map((n,i)=>({n,i})).filter(({n})=>(!rawLabel(n)||find('.sat-raw-toggle')?.checked)&&(!q||(name(n)+' '+(n.sub||'')+' '+(n.a||'')).toLowerCase().includes(q)));hits.sort(order==='name'?(a,b)=>name(a.n).localeCompare(name(b.n)):order==='links'?(a,b)=>(adj.get(b.i)?.length||0)-(adj.get(a.i)?.length||0):(a,b)=>(+K.size(b.n)||0)-(+K.size(a.n)||0));find('.sat-count').textContent=number(hits.length)+' entries'+(hits.length>limit?' · showing '+limit:'');find('.sat-more').hidden=hits.length<=limit;find('.sat-list').innerHTML=hits.slice(0,limit).map(({n,i})=>`<button class="sat-entry" data-entry="${i}" aria-pressed="${selected===i}"><strong>${escape(name(n))}</strong><span>${escape(sub(n))} · ${number(adj.get(i)?.length)} connections</span></button>`).join('')||'<p>No matching entries. Try another name or phrase.</p>';};
+ host.innerHTML=`<div class="sat-head"><h3>${doctrine?(data.relation?'Topics found together':'Topics in the patrology index'):kind==='works'?'Works with similar Scripture use':'Authors with similar Scripture use'}</h3><p>${doctrine?(data.relation?'Explore subjects with similar patterns of recorded neighbouring topics.':'Explore the subject entries and supplied links in the published patrology index.'):'Choose '+(kind==='works'?'a work':'an author')+' to compare Scripture references or read the available sources.'}</p><p class="sat-coverage">${number(nodes.length)} mapped ${kind==='doctrines'?'topics':kind}${data.shelf?' · '+escape(data.shelf):''}${data.version?' · Index dated '+escape(data.version):''}${data.capped?' · '+number(data.capped)+' works excluded by the published map’s cap':''}</p><details class="sat-method"><summary>What these connections mean</summary><p>${!data.relation?'This historical index links published entries. Its export does not declare how each weight was calculated. Open Why connected to inspect the supplied endpoint evidence.':doctrine?'Edges compare which other topics occur alongside each subject. Similar profiles suggest places to investigate; they do not establish a shared conclusion.':'Edges represent the published similarity of Scripture citation profiles. Similarity does not show that these authors read one another or agree.'} The diagram displays up to six recorded neighbours; the list includes every edge available for the selected entry. Counts and samples cover this export, not necessarily the full shelf.</p></details></div><div class="sat-layout"><section class="sat-index" aria-label="Constellation index"><label>Find ${kind==='works'?'a work':doctrine?'a topic':'an author'}<input class="sat-find" type="search" placeholder="Search this constellation"></label><label>Order entries<select class="sat-sort"><option value="weight">${doctrine?'Highest index weight':'Most recorded citations'}</option><option value="name">Name A–Z</option><option value="links">Most connections</option></select></label>${rawCount?`<label class="sat-raw"><input class="sat-raw-toggle" type="checkbox">Include ${rawCount} raw index labels</label><p class="sat-note">Raw labels contain extraction notes and need editorial review.</p>`:''}<p class="sat-count" role="status"></p><div class="sat-list"></div><nav class="ce-pager" aria-label="Entry pages"><button class="sat-previous">Previous</button><span class="sat-page"></span><button class="sat-more">Next entries</button></nav></section><section class="sat-detail" aria-label="Selected entry"><div class="sat-empty"><h4>Choose an entry to begin</h4><p>Compare the Scripture references behind a similarity, browse the available works, or follow recorded citations.</p><a href="/web">Explore the citation atlas</a></div></section></div>`;
+ const find=s=>host.querySelector(s);find('.sat-find').value=ctx.query||'';const drawList=()=>{const q=find('.sat-find').value.trim().toLowerCase(),order=find('.sat-sort').value,hits=nodes.map((n,i)=>({n,i})).filter(({n})=>(!rawLabel(n)||find('.sat-raw-toggle')?.checked)&&(!q||(name(n)+' '+(n.sub||'')+' '+(n.a||'')).toLowerCase().includes(q)));hits.sort(order==='name'?(a,b)=>name(a.n).localeCompare(name(b.n)):order==='links'?(a,b)=>(adj.get(b.i)?.length||0)-(adj.get(a.i)?.length||0):(a,b)=>(+K.size(b.n)||0)-(+K.size(a.n)||0));const win=windowOf(hits,page,pageSize);page=win.page;find('.sat-count').textContent=number(hits.length)+' entries';find('.sat-page').textContent='Page '+(page+1)+' of '+win.pages;find('.sat-previous').disabled=page===0;find('.sat-more').disabled=page+1===win.pages;find('.sat-list').innerHTML=win.items.map(({n,i})=>`<button class="sat-entry" data-entry="${i}" aria-pressed="${selected===i}"><strong>${escape(name(n))}</strong><span>${escape(sub(n))} · ${number(adj.get(i)?.length)} connections</span></button>`).join('')||'<p>No matching entries. Try another name or phrase.</p>';};
  async function rowsFor(i){if(K.rows)return {rows:await K.rows(nodes[i],i,en),fallback:false};if(!ctx.rowsUrl)return {rows:nodes[i].rows||[],fallback:false};if(!rowsPromise)rowsPromise=fetch(ctx.rowsUrl+(ctx.ver||'')).then(r=>{if(!r.ok)throw Error();return r.json();}).catch(()=>null);const d=await rowsPromise;return {rows:d?.r?.[i]||nodes[i].rows||[],fallback:!d};}
  function diagram(i,neighbours){const list=neighbours.slice(0,6),height=Math.max(160,list.length*66);const label=(text,x,y)=>{const words=text.split(' '),lines=[''];for(const word of words){if((lines.at(-1)+' '+word).length>29)lines.push(word);else lines[lines.length-1]+=(lines.at(-1)?' ':'')+word;}return `<text x="${x}" y="${y-(lines.length-1)*8}">${lines.map((l,j)=>`<tspan x="${x}" dy="${j?17:0}">${escape(l)}</tspan>`).join('')}</text>`;};return `<svg viewBox="0 0 720 ${height}" role="img" aria-label="${escape(name(nodes[i]))} and its ${list.length} strongest recorded connections"><g fill="none" stroke="currentColor" opacity=".45">${list.map((r,k)=>`<path d="M245 ${height/2} C335 ${height/2},335 ${33+k*66},420 ${33+k*66}"/>`).join('')}</g><circle cx="245" cy="${height/2}" r="5" fill="currentColor"/>${label(name(nodes[i]),8,height/2)}${list.map((r,k)=>`<circle cx="420" cy="${33+k*66}" r="4" fill="currentColor"/>${label(name(nodes[r.i]),434,33+k*66)}`).join('')}</svg>`;}
- async function open(i){const token=++sequence;selected=i;drawList();host.classList.add('sat-selected');const n=nodes[i],allNb=adj.get(i)||[],nb=allNb.filter(r=>!rawLabel(nodes[r.i])||find('.sat-raw-toggle')?.checked),detail=find('.sat-detail');const direct=K.href?.(n),ask=K.ask?.(n,name(n));
+ async function open(i,notify=true){const token=++sequence;selected=i;drawList();host.classList.add('sat-selected');const n=nodes[i],allNb=adj.get(i)||[],nb=allNb.filter(r=>!rawLabel(nodes[r.i])||find('.sat-raw-toggle')?.checked),detail=find('.sat-detail');const direct=K.href?.(n),ask=K.ask?.(n,name(n));if(notify)ctx.onSelect?.({entry:name(n),query:find('.sat-find').value,page});
  /* K.actions (2026-09-11, owner "change the usability of our connections
     between authors"): the calling page may add its OWN actions per entry —
     on /web the authors constellation now reaches the atlas's citation
     dossier (recorded citations with quoted passages), not just a works
     search. Entries: [{label, href}] or [{label, go:fn}]. */
  const extra=(K.actions?.(n,name(n))||[]).map((a,ai)=>a.href?`<a href="${escape(a.href)}">${escape(a.label)}</a>`:`<button class="sat-xact" data-xact="${ai}">${escape(a.label)}</button>`).join('');
- detail.innerHTML=`<button class="sat-back">Back to entries</button><h4 tabindex="-1">${escape(name(n))}</h4><p class="sat-meta">${escape(sub(n))}</p>${rawLabel(n)?'<p class="sat-note">This label contains an extraction note and needs editorial review. Source passages are preserved below.</p>':''}<div class="sat-actions">${direct?`<a href="${escape(direct)}">${escape(K.goLabel||'Open source')}</a>`:''}${extra}${doctrine?'<a class="sat-topic-link" href="/topics">Explore topic evidence</a>':''}${ask?`<a href="${escape(ask)}">Ask about this</a>`:''}</div>${nb.length?`<details class="sat-diagram"><summary>View the strongest connections</summary>${diagram(i,nb)}</details><h5>${nb.length} recorded connections${nb.length<allNb.length?' · '+(allNb.length-nb.length)+' raw labels hidden':''}</h5><p class="sat-note">Ordered by the published connection weight. The value is an index measure, not a probability.</p><div class="sat-neighbours">${nb.map(r=>`<div class="sat-neighbour"><button data-connection="${r.i}" aria-label="Why connected to ${escape(name(nodes[r.i]))}"><span>${escape(name(nodes[r.i]))}<small>Why connected</small></span><small>Weight ${Number(r.w).toLocaleString(undefined,{maximumFractionDigits:3})}</small></button><button data-entry="${r.i}" aria-label="Open ${escape(name(nodes[r.i]))}">Open entry</button></div>`).join('')}</div><div class="sat-connection-evidence"></div>`:'<p class="sat-note">No connecting edges are included for this entry.</p>'}<h5>${escape(K.rowLabel||'Source passages').replace('Every mined page','Available mined pages').replace('Nearest','Related')}</h5><p class="sat-source-count" role="status">Loading available sources…</p><label class="sat-evidence-label">Find a source or phrase<input class="sat-evidence-query" type="search" placeholder="Filter available sources"></label><div class="sat-sources"></div><button class="sat-source-more">Show more sources</button>`;
+ detail.innerHTML=`<button class="sat-back">Back to ${kind==='authors'?'authors':kind==='works'?'works':'topics'}</button><h4 tabindex="-1">${escape(name(n))}</h4><p class="sat-meta">${escape(sub(n))}</p>${rawLabel(n)?'<p class="sat-note">This label contains an extraction note and needs editorial review. Source passages are preserved below.</p>':''}<div class="sat-actions">${direct?`<a href="${escape(direct)}">${escape(K.goLabel||'Open source')}</a>`:''}${extra}${doctrine?'<a class="sat-topic-link" href="/topics">Explore topic evidence</a>':''}${ask?`<a href="${escape(ask)}">Ask about this</a>`:''}</div><div class="sat-detail-tabs" aria-label="Explore this entry"><button data-sat-tab="connections" aria-pressed="true">${doctrine?'Connections':'Compare Scripture'}</button><button data-sat-tab="sources" aria-pressed="false">${kind==='authors'?'Works':'Sources'}</button></div><section data-sat-panel="connections">${nb.length?`<div class="sat-connections-list"><details class="sat-diagram"><summary>View the strongest connections</summary>${diagram(i,nb)}</details><h5>${nb.length} ${doctrine?'topic connections':'similar profiles'}${nb.length<allNb.length?' · '+(allNb.length-nb.length)+' raw labels hidden':''}</h5><p class="sat-note">${doctrine?'Compare the indexed topics behind each connection.':'Select Compare Scripture to see which Bible chapters both profiles cite. Similarity alone does not establish agreement or a direct citation.'}</p><div class="sat-neighbours">${nb.map((r,index)=>`<div class="sat-neighbour" data-neighbour-page="${Math.floor(index/10)}"${index>=10?' hidden':''}><button data-connection="${r.i}" aria-label="Compare ${escape(name(n))} with ${escape(name(nodes[r.i]))}"><span>${escape(name(nodes[r.i]))}<small>${doctrine?'Compare topics':'Compare Scripture'}</small></span></button><button data-entry="${r.i}" aria-label="Open ${escape(name(nodes[r.i]))}">${kind==='authors'?'View author':'View entry'}</button></div>`).join('')}</div>${nb.length>10?`<nav class="ce-pager" aria-label="Similar profile pages"><button data-neighbour-prev disabled>Previous</button><span data-neighbour-count>1–10 of ${nb.length}</span><button data-neighbour-next>Next profiles</button></nav>`:''}</div><button class="sat-comparison-back" hidden>Back to connections</button><div class="sat-connection-evidence"></div>`:'<p class="sat-note">No connecting edges are included for this entry.</p>'}</section><section data-sat-panel="sources" hidden><h5>${escape(K.rowLabel||'Source passages').replace('Every mined page','Available mined pages').replace('Nearest','Related')}</h5><p class="sat-source-count" role="status">Loading available sources…</p><label class="sat-evidence-label">Find a source or phrase<input class="sat-evidence-query" type="search" placeholder="Filter available sources"></label><div class="sat-sources"></div><nav class="ce-pager" aria-label="Source pages"><button class="sat-source-previous">Previous</button><span class="sat-source-page"></span><button class="sat-source-more">Next sources</button></nav></section>`;
  detail.querySelectorAll('.sat-xact').forEach(b=>{const a=(K.actions?.(n,name(n))||[])[+b.dataset.xact];if(a&&a.go)b.onclick=()=>a.go();});
- detail.querySelector('.sat-back').onclick=()=>{host.classList.remove('sat-selected');find(`[data-entry="${i}"]`)?.focus();};detail.querySelector('h4').focus({preventScroll:!matchMedia('(max-width:760px)').matches});
+ detail.querySelectorAll('[data-sat-tab]').forEach(button=>button.onclick=()=>{const tab=button.dataset.satTab;detail.querySelectorAll('[data-sat-panel]').forEach(panel=>panel.hidden=panel.dataset.satPanel!==tab);detail.querySelectorAll('[data-sat-tab]').forEach(b=>b.setAttribute('aria-pressed',b===button));});
+ detail.querySelector('.sat-back').onclick=()=>{host.classList.remove('sat-selected');ctx.onSelect?.({entry:'',query:find('.sat-find').value,page});find(`[data-entry="${i}"]`)?.focus();};detail.querySelector('h4').focus({preventScroll:!matchMedia('(max-width:760px)').matches});
+ let neighbourPage=0;const drawNeighbours=()=>{detail.querySelectorAll('[data-neighbour-page]').forEach(row=>row.hidden=+row.dataset.neighbourPage!==neighbourPage);detail.querySelector('[data-neighbour-count]').textContent=(neighbourPage*10+1)+'–'+Math.min(nb.length,(neighbourPage+1)*10)+' of '+nb.length;detail.querySelector('[data-neighbour-prev]').disabled=neighbourPage===0;detail.querySelector('[data-neighbour-next]').disabled=(neighbourPage+1)*10>=nb.length;};detail.querySelector('[data-neighbour-prev]')?.addEventListener('click',()=>{neighbourPage--;drawNeighbours();});detail.querySelector('[data-neighbour-next]')?.addEventListener('click',()=>{neighbourPage++;drawNeighbours();});
  let comparisonSequence=0;
  detail.querySelectorAll('[data-connection]').forEach(button=>button.onclick=async()=>{
    const comparisonToken=++comparisonSequence,j=+button.dataset.connection,box=detail.querySelector('.sat-connection-evidence'),edge=nb.find(r=>r.i===j);
+   detail.querySelector('.sat-connections-list').hidden=true;const back=detail.querySelector('.sat-comparison-back');back.hidden=false;back.onclick=()=>{++comparisonSequence;box.innerHTML='';back.hidden=true;detail.querySelector('.sat-connections-list').hidden=false;button.focus();};back.focus();
    box.innerHTML='<p class="sat-note" role="status">Loading the evidence behind this connection…</p>';
    const results=await Promise.allSettled([rowsFor(i),rowsFor(j)]);if(token!==sequence||comparisonToken!==comparisonSequence||!host.isConnected)return;
    const a=results[0].status==='fulfilled'?results[0].value:{rows:n.rows||[],fallback:true},b=results[1].status==='fulfilled'?results[1].value:{rows:nodes[j].rows||[],fallback:true};
@@ -49,9 +52,9 @@
    if(token===sequence&&comparisonToken===comparisonSequence)box.querySelector('h5')?.focus({preventScroll:false});
  });
  if(doctrine){fetch(ctx.blob+'/v1/mine/topic2-all/index.json').then(r=>r.json()).then(d=>{if(token!==sequence)return;const t=(d.topics||[]).find(t=>t.t===name(n));if(t)detail.querySelector('.sat-topic-link').href='/topics#'+encodeURIComponent(t.s);}).catch(()=>{});}
- let result;try{result=await rowsFor(i);}catch(_){result={rows:n.rows||[],fallback:true};}if(token!==sequence||!host.isConnected)return;let shown=24;let rows=result.rows;if(kind==='works'&&rows.some(r=>!r.h)){try{const books=await fetch(ctx.blob+'/v1/bible/all/books.json').then(r=>r.json());if(token!==sequence)return;rows=rows.map(r=>{const h=!r.h&&window.FRShelfMap.chapterURL(r.t,books.books);return h?{...r,h,chapterLink:true}:r;});}catch(_){}}
- const drawRows=()=>{const q=detail.querySelector('.sat-evidence-query').value.trim().toLowerCase(),hits=rows.filter(r=>!q||[r.t,r.s,r.g].join(' ').toLowerCase().includes(q));detail.querySelector('.sat-source-count').textContent='Showing '+number(Math.min(shown,hits.length))+' of '+number(hits.length)+' available sources'+(n.more?' · '+number(n.more)+' further records counted outside this export':'')+(result.fallback?' · Larger source export could not load; showing the embedded sample.':'');detail.querySelector('.sat-source-more').hidden=shown>=hits.length;detail.querySelector('.sat-sources').innerHTML=hits.slice(0,shown).map(r=>`<article><h6>${r.h?`<a href="${escape(r.h)}">${escape(r.t||'Open passage')}</a>`:escape(r.t||'Source')}</h6>${r.s?`<p class="sat-note">${escape(r.s)}</p>`:''}${r.g?`<p>${escape(r.g)}</p>`:''}${r.h?`<a class="sat-read" href="${escape(r.h)}">${r.chapterLink?'Read Bible chapter':'Read source'}</a>${window.FRShelfMap.previewHTML(r.h)}`:''}</article>`).join('')||'<p class="sat-note">No available source matches. Clear the phrase or use the source link above.</p>';window.FRShelfMap.bindPreviews(detail.querySelector('.sat-sources'));};detail.querySelector('.sat-evidence-query').oninput=()=>{shown=24;drawRows();};detail.querySelector('.sat-source-more').onclick=()=>{shown+=24;drawRows();};drawRows();
+ let result;try{result=await rowsFor(i);}catch(_){result={rows:n.rows||[],fallback:true};}if(token!==sequence||!host.isConnected)return;let sourcePage=0;let rows=result.rows;if(kind==='authors'){try{const corpus=await window.FRResearchData.corpus();if(token!==sequence)return;rows=rows.map(r=>{const slug=r.h?new URL(r.h,location.href).searchParams.get('w'):null,work=corpus.works.get(slug);return work?{...r,t:work.title_en||work.title||r.t}:r;});}catch(_){}}if(kind==='works'&&rows.some(r=>!r.h)){try{const books=await fetch(ctx.blob+'/v1/bible/all/books.json').then(r=>r.json());if(token!==sequence)return;rows=rows.map(r=>{const h=!r.h&&window.FRShelfMap.chapterURL(r.t,books.books);return h?{...r,h,chapterLink:true}:r;});}catch(_){}}
+ const drawRows=()=>{const q=detail.querySelector('.sat-evidence-query').value.trim().toLowerCase(),hits=rows.filter(r=>!q||[r.t,r.s,r.g].join(' ').toLowerCase().includes(q));const win=windowOf(hits,sourcePage,8);sourcePage=win.page;detail.querySelector('.sat-source-count').textContent=number(hits.length)+' available sources'+(n.more?' · '+number(n.more)+' further records counted outside this export':'')+(result.fallback?' · Larger source export could not load; showing the embedded sample.':'');detail.querySelector('.sat-source-page').textContent='Page '+(sourcePage+1)+' of '+win.pages;detail.querySelector('.sat-source-previous').disabled=sourcePage===0;detail.querySelector('.sat-source-more').disabled=sourcePage+1===win.pages;detail.querySelector('.sat-sources').innerHTML=win.items.map(r=>`<article><h6>${r.h?`<a href="${escape(r.h)}" target="_blank" rel="noopener">${escape(r.t||'Open passage')}</a>`:escape(r.t||'Source')}</h6>${r.s?`<p class="sat-note">${escape(r.s.replace(/\bmined pages\b/g,'indexed pages'))}</p>`:''}${r.g?`<p>${escape(r.g)}</p>`:''}${r.h?`<a class="sat-read" href="${escape(r.h)}" target="_blank" rel="noopener">${r.chapterLink?'Read Bible chapter':kind==='authors'?'Open work':'Open source'}</a>${window.FRShelfMap.previewHTML(r.h)}`:''}</article>`).join('')||'<p class="sat-note">No available source matches. Clear the phrase or use the source link above.</p>';window.FRShelfMap.bindPreviews(detail.querySelector('.sat-sources'));};detail.querySelector('.sat-evidence-query').oninput=()=>{sourcePage=0;drawRows();};detail.querySelector('.sat-source-previous').onclick=()=>{sourcePage--;drawRows();};detail.querySelector('.sat-source-more').onclick=()=>{sourcePage++;drawRows();};drawRows();
  }
- host.addEventListener('click',e=>{const el=e.target.closest('[data-entry]');if(el&&host.contains(el))open(+el.dataset.entry);});if(find('.sat-raw-toggle'))find('.sat-raw-toggle').onchange=()=>{limit=30;drawList();if(selected>=0&&rawLabel(nodes[selected])&&!find('.sat-raw-toggle').checked){++sequence;selected=-1;host.classList.remove('sat-selected');find('.sat-detail').innerHTML='<p class="sat-note">Choose an entry to explore its sources and connections.</p>';drawList();find('.sat-find').focus();}else if(selected>=0)open(selected);};find('.sat-find').oninput=()=>{limit=30;drawList();};find('.sat-sort').onchange=()=>{limit=30;drawList();};find('.sat-more').onclick=()=>{limit+=30;drawList();};drawList();
- if(ctx.focus){const i=nodes.findIndex(n=>name(n).toLowerCase()===String(ctx.focus).toLowerCase());if(i>=0)open(i);}
+ host.addEventListener('click',e=>{const el=e.target.closest('[data-entry]');if(el&&host.contains(el))open(+el.dataset.entry);});if(find('.sat-raw-toggle'))find('.sat-raw-toggle').onchange=()=>{page=0;drawList();if(selected>=0&&rawLabel(nodes[selected])&&!find('.sat-raw-toggle').checked){++sequence;selected=-1;host.classList.remove('sat-selected');find('.sat-detail').innerHTML='<p class="sat-note">Choose an entry to explore its sources and connections.</p>';drawList();find('.sat-find').focus();}else if(selected>=0)open(selected);};find('.sat-find').oninput=()=>{page=0;drawList();};find('.sat-sort').onchange=()=>{page=0;drawList();};find('.sat-previous').onclick=()=>{page--;drawList();};find('.sat-more').onclick=()=>{page++;drawList();};drawList();
+ if(ctx.focus){const i=nodes.findIndex(n=>name(n).toLowerCase()===String(ctx.focus).toLowerCase());if(i>=0)open(i,false);}
 }};
--- a/tools/prdl_reader_prototype/web_shell.html
+++ b/tools/prdl_reader_prototype/web_shell.html
@@ -223,7 +223,7 @@
 <link rel="stylesheet" href="/constellations.css?v=__RESEARCH_V__">
 <link rel="stylesheet" href="/shelf-constellations.css"><script src="/shelf-constellations.js"></script>
 <script src="/research-tools.js?v=__RESEARCH_V__"></script>
-<script src="/scripture-tools.js"></script>
+<script src="/scripture-tools.js?v=__TOOLS_V__"></script>
 </head>
 <body class="web-page" data-view="browse">
 <!-- THESIS: A citation atlas for finding relationships and reading their evidence.
@@ -943,7 +943,7 @@
 
 /* ── verse 360 ──────────────────────────────────────────────────────────────── */
 let BOOKS=null,COMMS=null;
-async function openVerse(book,ch,push=true){
+async function openVerse(book,ch,push=true,options={}){
   const token=++__PSEQ;
   if(push)history.pushState(null,'','#v='+book+'/'+ch);
   openPanel('Scripture','Loading chapter connections…');pbody.innerHTML='<p class="loading" role="status">Loading Scripture…</p>';
@@ -956,9 +956,9 @@
   const books=BOOKS?.books||[],B=books.find(b=>b.slug===book),chapters=B?.chapters||[];
   const controls=`<div class="web-scripture-nav"><label>Book<select id="pkb" class="pk">${books.map(b=>`<option value="${esc(b.slug)}"${b.slug===book?' selected':''}>${esc(b.book)}</option>`).join('')}</select></label><label>Chapter<select id="pkc" class="pk">${chapters.map(x=>`<option value="${x.c}"${x.c===+ch?' selected':''}>${x.c}</option>`).join('')}</select></label></div>`;
   openPanel(`${esc(d.book)} ${ch}`,`${names.length} authors in the indexed passages · ${slugged.length} shown on the graph`);$('#map-title').textContent=d.book+' '+ch;$('#map-summary').textContent=slugged.length+' authors with indexed chapter citations';
-  const vs=(d.verses||[]).map(v=>{const rows=v.rows||[],authors=new Map();rows.forEach(r=>{if(!authors.has(r.a))authors.set(r.a,r);});return `<article class="vrow"><div class="vt"><a class="vn" href="${FRScripture.bibleURL(book,ch,v.v)}" aria-label="Read ${esc(d.book)} ${ch}:${v.v}">${v.v}</a>${esc(v.t||'')}</div>${rows.length?`<details class="web-verse-sources"><summary>${authors.size} authors · ${rows.length} indexed passages</summary>${[...authors].map(([name,r])=>`<div class="web-verse-source"><span>${esc(name)}</span><a href="${FRScripture.readerURL(r.w,r.p)}" target="_blank" rel="noopener">Read source</a></div>`).join('')}<a class="web-all-citations" href="${FRScripture.bibleURL(book,ch,v.v)}">Explore all citations for verse ${v.v}</a></details>`:''}</article>`;}).join('');
-  pbody.innerHTML=`${controls}<nav class="web-chapter-links"><a href="${FRScripture.bibleURL(book,ch)}">Read chapter</a><a href="${FRScripture.bibleURL(book,ch,null,'commentaries')}">Commentaries</a><a href="${FRScripture.bibleURL(book,ch,null,'annotations')}">Whole-Bible annotations</a></nav><p class="scripture-status">Select an author on the graph to trace their connections, or open a verse’s sources below.</p>${vs}`;
+  pbody.innerHTML=`${controls}<nav class="web-chapter-links"><a href="${FRScripture.bibleURL(book,ch)}" target="_blank" rel="noopener">Read chapter</a><a href="${FRScripture.bibleURL(book,ch,null,'commentaries')}" target="_blank" rel="noopener">Commentaries</a><a href="${FRScripture.bibleURL(book,ch,null,'annotations')}" target="_blank" rel="noopener">Whole-Bible annotations</a></nav><p class="scripture-status">Choose a verse, then expand an author to read the sources. The map shows authors represented in this chapter.</p><div id="web-verse-evidence"><p class="scripture-status" role="status">Loading source works…</p></div>`;
   $('#pkb').onchange=e=>openVerse(e.target.value,1);$('#pkc').onchange=e=>openVerse(book,+e.target.value);pbody.scrollTop=0;
+  await FRScripture.renderWebVerse($('#web-verse-evidence'),d,{book,ch,...options,saveBtn,onChange:state=>{if(token===__PSEQ)history.replaceState(null,'',FRScripture.webVerseURL(book,ch,state));}});
 }
 function openChainPicker(chain){
  ++__PSEQ;openPanel('Extend this path',chain.map(s=>esc(BYS[s]?.a||s)).join(' → '));
@@ -992,21 +992,21 @@
   if(m==="topic")openTopicIndex();
   if(m==="verse")openVerse("romans",8);
 });
-async function openShelfMaps(shelf='english-divines',kind='authors',push=true){
+async function openShelfMaps(shelf='english-divines',kind='authors',push=true,options={}){
  const token=++__PSEQ;if(push)history.pushState(null,'','#shelves='+encodeURIComponent(shelf)+'/'+kind);
  openPanel('Shelf connections','Explore Scripture citation patterns and topics recorded together.');document.body.classList.add('shelf-comparison');$('#atlas').inert=true;$('#explorer').inert=true;pbody.innerHTML='<p class="loading" role="status">Loading shelf maps…</p>';
  const idx=await J(BLOB+'/v1/mine/constellations/index.json').catch(()=>null);if(token!==__PSEQ)return;
  const shelves=idx?.shelves||[],entry=shelves.find(s=>s.slug===shelf||s.s===shelf)||shelves.find(s=>s.shelf==='English Divines')||shelves[0];
  if(!entry){pbody.innerHTML='<p class="index-note">Shelf maps could not load.</p><button class="pbtn" id="shelf-retry">Retry shelf maps</button>';$('#shelf-retry').onclick=()=>openShelfMaps(shelf,kind,false);return;}
  const slugOf=s=>s.slug||s.s||s.shelf.toLowerCase().replace(/[^a-z0-9]+/g,'-'),sl=slugOf(entry),labels={authors:'Authors',works:'Works',doctrines:'Topics'},available=Object.keys(labels).filter(k=>Object.prototype.hasOwnProperty.call(entry.have||{},k));
- if(!available.includes(kind))kind=available[0]||'';history.replaceState(null,'','#shelves='+encodeURIComponent(sl)+(kind?'/'+kind:''));
+ if(!available.includes(kind))kind=available[0]||'';history.replaceState(null,'','#shelves='+encodeURIComponent(sl)+(kind?'/'+kind:'')+((options.entry||options.query||options.page)?'?'+new URLSearchParams({entry:options.entry||'',q:options.query||'',page:options.page||0}):''));
  pbody.innerHTML=`<div class="shelf-map-controls"><label>Shelf or group<select id="shelf-map-shelf">${shelves.map(s=>`<option value="${esc(slugOf(s))}">${esc(s.shelf)}</option>`).join('')}</select></label><label>Explore connections between<select id="shelf-map-kind"${available.length?"":" disabled"}>${available.length?available.map(k=>`<option value="${k}">${labels[k]}</option>`).join(''):'<option>No maps published</option>'}</select></label></div><div id="shelf-map-host"><p class="loading" role="status">Loading this constellation…</p></div>`;
  $('#shelf-map-shelf').value=sl;$('#shelf-map-kind').value=kind;$('#shelf-map-shelf').onchange=e=>openShelfMaps(e.target.value,kind);$('#shelf-map-kind').onchange=e=>openShelfMaps(sl,e.target.value);
  if(!available.length){$('#shelf-map-host').innerHTML='<p class="index-note">No constellation exports are published for this group. Choose another shelf or group.</p>';return;}
  const base=BLOB+'/v1/mine/constellations/'+sl+'/',d=await J(base+kind+'.json').catch(()=>null);if(token!==__PSEQ)return;
  if(!d){$('#shelf-map-host').innerHTML='<p class="index-note">This constellation could not load. Your shelf and map selection are kept.</p><button class="pbtn" id="shelf-graph-retry">Retry constellation</button>';$('#shelf-graph-retry').onclick=()=>openShelfMaps(sl,kind,false);return;}
  if(!d.nodes?.length){$('#shelf-map-host').innerHTML='<p class="index-note">This export contains no '+(kind==='doctrines'?'topics':kind)+'. Choose another map or shelf.</p>';return;}
- const K={name:n=>n.t||n.a,size:n=>n.n,unit:'Scripture citations',href:n=>kind==='works'&&n.w?FRScripture.readerURL(n.w):kind==='authors'?'/fathers?q='+encodeURIComponent(n.a):'',goLabel:kind==='works'?'Read work':'Find author works',rowLabel:kind==='authors'?'Available works':kind==='works'?'Recorded Scripture chapters':'Available topic passages',ask:(n,nm)=>'/ask?q='+encodeURIComponent('Explore '+nm+' in '+entry.shelf)};
+ const K={name:n=>n.t||n.a,size:n=>n.n,unit:'Scripture citations',href:n=>kind==='works'&&n.w?FRScripture.readerURL(n.w):kind==='authors'?'/fathers?q='+encodeURIComponent(n.a):'',goLabel:kind==='works'?'Open work':'View works',rowLabel:kind==='authors'?'Available works':kind==='works'?'Recorded Scripture chapters':'Available topic passages',ask:(n,nm)=>'/ask?q='+encodeURIComponent('Explore '+nm+' in '+entry.shelf)};
  /* CONNECTIONS YOU CAN ACT ON (owner 2026-09-11 "change the usability of our
     connections between authors"): the constellation's similarity edges now
     reach the atlas's REAL evidence. Every author entry links its citation
@@ -1021,7 +1021,8 @@
      finishes loading — an eager map would be empty forever) */
   let _byName=null,_byNameN=0;
   const atlasSlug=nm=>{if(!_byName||_byNameN!==NODES.length){_byName={};_byNameN=NODES.length;NODES.forEach(x=>{if(x&&x.a)_byName[_fold(x.a)]=x.s;});}return _byName[_fold(nm)]||null;};
-  K.actions=(n,nm)=>{const s2=atlasSlug(nm);return s2?[{label:'Citation dossier · who cites them, with passages',go:()=>openAuthor(s2)}]:[];};
+  /* Citation dossier: the existing author evidence route is retained behind Follow citations. */
+  K.actions=(n,nm)=>{const s2=atlasSlug(nm);return s2?[{label:'Follow citations',go:()=>openAuthor(s2)}]:[];};
   K.pairActions=(nA,nB)=>{
     const a2=atlasSlug(nA.t||nA.a),b2=atlasSlug(nB.t||nB.a),acts=[];
     if(a2&&b2){const pu=pairURL(a2,b2);if(pu)acts.push({label:'Full comparison · both directions, shared topics',href:pu});
@@ -1032,7 +1033,7 @@
         if(!ab&&!ba)acts.push({label:'No recorded citation between them — similarity only',href:null,note:1});}}
     return acts.filter(x=>x.go||x.href);};
  }
- FRShelfMap.render($('#shelf-map-host'),d,K,{kind,shelf:sl,rowsUrl:base+kind+'.rows.json',blob:BLOB});pbody.scrollTop=0;
+ FRShelfMap.render($('#shelf-map-host'),d,K,{kind,shelf:sl,rowsUrl:base+kind+'.rows.json',blob:BLOB,focus:options.entry,query:options.query,page:options.page,onSelect:state=>{if(token===__PSEQ)history.pushState(null,'','#shelves='+encodeURIComponent(sl)+'/'+kind+'?'+new URLSearchParams({entry:state.entry,q:state.query,page:state.page}));}});pbody.scrollTop=0;
 }
 
 async function openTopicIndex(push=true){
@@ -1050,7 +1051,7 @@
 function route(){
   stashJourney();const h=location.hash.slice(1);const kind=h.split("=")[0],mode=({v:"verse",t:"topic",topics:"topic",shelves:"shelves",paths:"path",p:"path",journey:"path"})[kind]||"sky";document.querySelectorAll("#mnav [data-m]").forEach(a=>a.classList.toggle("on",a.dataset.m===mode));
   if(!h){$("#atlas").inert=false;$("#explorer").inert=false;document.body.classList.remove("shelf-comparison");++__PSEQ;panel.inert=true;panel.classList.remove("open");document.body.classList.remove("popen");FOCUS=null;SUBSET=null;PATHV=null;window.__JDIR=null;renderAuthorIndex();requestAnimationFrame(resize);draw();return;}
-  if(h==="shelves"||h.startsWith("shelves=")){const [sh,kind]=(h.split("=")[1]||"english-divines/authors").split("/");openShelfMaps(decodeURIComponent(sh),kind||"authors",false);return;}
+  if(h==="shelves"||h.startsWith("shelves=")){const [path,query=""]=(h.startsWith("shelves=")?h.slice(8):"english-divines/authors").split("?"),[sh,kind]=path.split("/"),params=new URLSearchParams(query);openShelfMaps(decodeURIComponent(sh),kind||"authors",false,{entry:params.get("entry")||"",query:params.get("q")||"",page:+params.get("page")||0});return;}
   if(h.startsWith('journey=')){const [path,query='']=h.slice(8).split('?'),params=new URLSearchParams(query);openJourney(path.split(',').filter(Boolean).map(decodeURIComponent),params.get('direction')||'in',{work:params.get('work')||'',targetWork:params.get('targetWork')||'',reference:params.get('reference')||'',via:(params.get('via')||'').split(',').filter(Boolean),push:false});return;}
   if(h==="paths"){openPathPicker(false);return;}if(h==="topics"){openTopicIndex(false);return;}
   const [k,v]=h.split("=");
@@ -1064,7 +1065,7 @@
   else if(k==="p"&&v){const st=v.split(",").filter(Boolean);if(st.length>=2)openPath(st,false);}
   else if(k==="e"&&v){const [from,to]=v.split(",").map(decodeURIComponent);if(from&&to)openEdge(from,to,null,false);}
   else if(k==="t"&&v)openTopic(v,false);
-  else if(k==="v"&&v){const [bk,ch]=v.split("/");openVerse(bk,+ch||1,false);}
+  else if(k==="v"&&v){const [path,query='']=h.slice(2).split('?'),[bk,ch]=path.split('/'),params=new URLSearchParams(query);openVerse(bk,+ch||1,false,{verse:params.get('verse'),author:params.get('author')||'',query:params.get('q')||'',page:+params.get('page')||0});}
 }
 addEventListener("hashchange",route);
 addEventListener("popstate",route);
