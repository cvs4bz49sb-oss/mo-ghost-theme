> See `../agents/FRONTEND-AGENT.md` (in the MOCA `website/agents/` folder). That's the canonical version.

This file is a pointer so commands like `/frontend-check` running from inside the theme repo can find the agent's constitution.
