{{!< default}}
{{#contentFor "moHead"}}
<title>Ask | The Faith Received</title>
<meta name="description" content="Ask a question of the library. The answer is written from the corpus, with every claim cited to a page you can open." />
<meta property="og:title" content="Ask | The Faith Received" />
<meta property="og:description" content="Ask a question of the library. The answer is written from the corpus, with every claim cited to a page you can open." />
<meta name="twitter:title" content="Ask | The Faith Received" />
<meta name="twitter:description" content="Ask a question of the library. The answer is written from the corpus, with every claim cited to a page you can open." />
{{/contentFor}}
{{!-- /the-faith-received/ask/ — a question answered from the library,
     with inline citations that resolve to a real reader page. Talks
     to the mo-tfr-library Worker's POST /v1/ask (see
     website/workers/tfr-library/lib/ask.js). Rendered as an editorial
     set piece, per the data owner's spec: the question becomes a
     heading, the answer is running prose, and citations are footnotes
     linking into the reader — not a chat bubble.

     This is the standalone route. The same Ask UI is also embedded
     inline as the "Ask" tab on /the-faith-received/search/
     (custom-faith-search.hbs, [data-fs-panel="ask"]) — both pages
     render partials/faith-received/_ask-panel.hbs rather than each
     carrying its own copy, since assets/js/page/faith-ask.js binds to
     that markup with plain unscoped querySelector() calls regardless
     of which page loaded it. See that partial's own header comment for
     the DOM contract and the required script-loading order. This page
     stays a separate route (rather than folding entirely into the
     search page) for its own dedicated hero, URL, and metadata.

     Layout redesigned 2026-09 (second pass) into a full-bleed
     workspace: .ask-workspace breaks out of the container below and
     runs the full width of the viewport on its own white ground, so
     the page opens into it rather than holding it. The band it sits in
     carries `faith-ask-band`, which zeroes the band's top padding so
     the window butts straight up against the dark hero — that join is
     the "pop into it" moment and it does not survive 92px of page
     ground between the two. See "Ask: the research workspace" in
     assets/css/faith-received.css for the full reasoning. --}}

<main class="faith-received faith-feature">

  <section class="bhero bhero--tool">
    <div class="bhero-inner container">
      <p class="bhero-kicker"><a href="/the-faith-received/">The Faith Received</a> <span aria-hidden="true">&middot;</span> <a href="/the-faith-received/ask/">Ask</a></p>
      <h1>Ask</h1>
      <p class="bhero-sub">Ask a question of the library. The answer is written from the corpus itself. Every claim is cited to a page you can open.</p>
    </div>
  </section>

  {{!-- BETA GATE. Ask is free during the beta and requires an account,
       where "account" is any signed-in Ghost member, free included.
       Signed-out visitors get the signup box instead of the workspace;
       the markup below is not served to them at all. The contract is
       written out in partials/faith-received/_beta-gate.hbs. --}}
  {{#if @member}}
  <section class="bband faith-feature-body faith-ask-band">
    <div class="container" data-ask-page>

      {{!-- Shared with the "Ask" tab on /the-faith-received/search/ —
           see partials/faith-received/_ask-panel.hbs for the markup,
           the DOM contract faith-ask.js depends on, and the required
           script-loading order. --}}
      {{> "faith-received/_ask-panel"}}

    </div>
  </section>
  {{/if}}

  {{> "faith-received/_beta-gate"}}


<section class="faith-join">
  <div class="container">
    <p class="blabel">Become a member</p>
    <h2>Use the tools and support the work.</h2>
      <div class="toggle-wrap" role="tablist" aria-label="Billing interval">
        <div class="toggle">
          <button type="button" class="toggle-option is-active" data-interval="annual" role="tab" aria-selected="true" data-hm-goal="plan-annual">
            Annual
            <span class="badge">Save 17%</span>
          </button>
          <button type="button" class="toggle-option" data-interval="monthly" role="tab" aria-selected="false" data-hm-goal="plan-monthly">Monthly</button>
        </div>
      </div>
  </div>
</section>

{{> "membership-pricing"}}
{{> "faith-received/_beta-band"}}
</main>
{{!-- The sitewide membership CTA used to sit here. It was pulled on
     2026-09-11 by Ian: its copy is essay-specific ("Loved this essay?
     Help us write the next one"), which reads as nonsense at the foot
     of a research tool, and on this page it was the third membership
     pitch in a row. The merged band above already names what membership
     buys and says the tools become Member features after the beta, and
     the pricing cards sit between the two.

     It is STILL on the twenty browse, reading-room and shelf pages,
     where it is the only membership prompt there is. Those need a
     replacement written for a library rather than a deletion. --}}

<script src="{{asset "js/page/membership-toggle.js"}}"></script>
<script src="{{asset "js/page/membership-cards.js"}}"></script>

{{!-- Claims the TFR / TFR-Beta Kit tags for a signed-in member. Hides
     and reveals nothing; see the file header. --}}
<script src="{{asset "js/faith-beta-gate.js"}}"></script>

{{!-- The composer's two scripts ride inside the same member branch as
     the panel they bind to. A signed-out visitor has no composer. --}}
{{#if @member}}
<script src="{{asset "js/feature-gate.js"}}"></script>
<script src="{{asset "js/port/ask-store.js"}}"></script>
<script src="{{asset "js/faith-ask-history.js"}}"></script>
<script src="{{asset "js/page/faith-ask.js"}}"></script>
{{/if}}
