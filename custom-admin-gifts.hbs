{{!< default}}
{{!-- /admin/members/gifts/ — every gift membership on the platform. --}}

<main class="admin-page admin-workspace">
  {{#if @member}}
    {{> "admin-sidebar" active="members"}}
    <div class="admin-main">
      <header class="admin-topbar">
        <h1 class="admin-topbar-title"><em>Gift Memberships</em></h1>
        <p class="admin-topbar-sub">Every gift membership on the platform. Download the CSV for reporting.</p>
      </header>

    <section class="admin-body">
      <div class="container">
        <div class="admin-members-layout">
          <aside class="admin-members-sidebar">
            {{> "admin-members-subnav" active="gifts"}}
          </aside>
          <div class="admin-members-main">
            {{!-- Gift action panel: resend the gift notification email
                 or copy a fresh sign-in link to paste into a direct reply.
                 Hydrated by admin-gifts-actions.js. --}}
            <div class="admin-gift-actions-panel" data-gift-actions
                 data-api-base="{{@custom.membership_api_base}}" hidden>
              <h2 class="admin-section-heading">Provisioned gifts</h2>
              <p class="admin-section-sub">Re-send the welcome email (generates a fresh sign-in link) or copy the link to paste into your own reply.</p>
              <ul class="admin-gift-list" data-gift-list></ul>
              <p class="admin-gift-actions-empty" data-gift-empty hidden>No provisioned gifts yet.</p>
            </div>
            <div class="admin-table-host"
                 data-admin-table
                 data-title="Gifts"
                 data-endpoint="/api/admin/gifts"
                 data-collection="gifts"
                 data-api-base="{{@custom.membership_api_base}}"
                 data-member-email="{{@member.email}}"
                 data-columns="tier,purchaser_name,recipient_name,recipient_email,status,comp_until,created_at"
                 data-column-labels="Tier,Purchaser,Recipient,Email,Status,Comp Until,Created">
              <div class="admin-table-toolbar">
                <p class="admin-table-count" data-count-label>Loading&hellip;</p>
                <a href="#" class="btn btn-primary btn-sm" data-download-csv hidden>Download CSV</a>
              </div>
              <p class="admin-table-status" data-status></p>
              <div class="admin-table-wrap">
                <table class="admin-table" data-table hidden>
                  <thead><tr data-thead></tr></thead>
                  <tbody data-tbody></tbody>
                </table>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    </div>
  {{else}}
    {{> "admin-denied"}}
  {{/if}}
</main>

<script src="{{asset "js/admin-auth.js"}}"></script>
<script src="{{asset "js/admin-sidebar.js"}}"></script>
<script src="{{asset "js/admin-table.js"}}"></script>
<script src="{{asset "js/admin-gifts-actions.js"}}"></script>
