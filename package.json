{
  "name": "mere-orthodoxy",
  "description": "Mere Orthodoxy — Christian renewal for the common good.",
  "version": "1.2.0",
  "engines": {
    "ghost": ">=5.0.0"
  },
  "license": "MIT",
  "author": {
    "name": "Ian Harber",
    "email": "iharber93@gmail.com"
  },
  "keywords": [
    "ghost",
    "theme",
    "ghost-theme"
  ],
  "config": {
    "posts_per_page": 12,
    "card_assets": true,
    "custom": {
      "hero_headline_line_1": {
        "type": "text",
        "default": "Christian Renewal"
      },
      "hero_headline_line_2": {
        "type": "text",
        "default": "for the"
      },
      "hero_headline_line_3": {
        "type": "text",
        "default": "Common Good."
      },
      "hero_subhead": {
        "type": "text",
        "default": "Long-form essays, a quarterly print journal, and patient podcasts on theology, culture, and spiritual formation."
      },
      "podcast_feed_url": {
        "type": "text",
        "description": "URL of the deployed podcast-feed Worker. Leave empty to hide the podcast section."
      },
      "digest_form_action": {
        "type": "text",
        "description": "Form action URL for the Weekly Digest signup (Kit, Mailchimp, ConvertKit, etc.)."
      },
      "journal_status_issue": {
        "type": "text",
        "default": "Spring 2026",
        "description": "Issue label shown in the Journal Status tracker on the homepage (e.g. 'Spring 2026'). Bump this when a new issue enters the pipeline."
      },
      "journal_status_stage": {
        "type": "select",
        "options": ["Drafting", "In Production", "Arriving"],
        "default": "Drafting",
        "description": "Current stage of the next journal issue. The matching dot glows; the other two fade. Advance manually — Drafting → In Production → Arriving — then reset to Drafting for the next issue."
      },
      "content_api_key": {
        "type": "text",
        "description": "Ghost Content API key for the homepage topic filter (optional)."
      },
      "stripe_member_annual_url": {
        "type": "text",
        "description": "Stripe Payment Link URL for the $100/yr Member tier. Create in Stripe → Payment Links. When set, the /membership Become-a-Member buttons link here instead of Ghost Portal."
      },
      "stripe_member_monthly_url": {
        "type": "text",
        "description": "Stripe Payment Link URL for the $10/mo Member tier. Used by the billing-interval toggle on /membership."
      },
      "stripe_lifetime_url": {
        "type": "text",
        "description": "Stripe Payment Link URL for the $995 Lifetime tier. Create in Stripe → Payment Links."
      },
      "gift_url": {
        "type": "text",
        "default": "/gift",
        "description": "URL for the gift membership flow."
      },
      "group_url": {
        "type": "text",
        "default": "/groups",
        "description": "URL for the group/organization membership flow."
      },
      "institution_url": {
        "type": "text",
        "default": "/institutions",
        "description": "URL for the seminary/institution membership flow."
      },
      "membership_api_base": {
        "type": "text",
        "default": "https://join.mereorthodoxy.com",
        "description": "Base URL for the membership-site Express app (Stripe + HubSpot endpoints)."
      },
      "podcast_mf_apple_url": {
        "type": "text",
        "description": "Apple Podcasts show URL for Mere Fidelity."
      },
      "podcast_mf_spotify_url": {
        "type": "text",
        "description": "Spotify show URL for Mere Fidelity."
      },
      "podcast_crc_apple_url": {
        "type": "text",
        "description": "Apple Podcasts show URL for Christians Reading Classics."
      },
      "podcast_crc_spotify_url": {
        "type": "text",
        "description": "Spotify show URL for Christians Reading Classics."
      },
      "digest_worker_url": {
        "type": "text",
        "description": "Full URL of the deployed mo-digest Cloudflare Worker (e.g. https://mo-digest.mo-podcast-feed.workers.dev). Consumed by /digest/."
      },
      "digest_auth_token": {
        "type": "text",
        "description": "Shared secret matching DIGEST_AUTH_TOKEN on the mo-digest Worker. When set, /digest/ auto-fills the auth field so you don't type it every time. Visible in page source — anyone who loads /digest/ can read it, so treat this page as semi-private."
      },
      "gate_days": {
        "type": "text",
        "default": "7",
        "description": "Days a new post stays unrestricted before the soft subscriber gate kicks in. 0 disables the gate entirely. Soft gate only — JS overlay on the rendered article; not a real paywall. Upgrade to a worker-enforced gate later."
      },
      "gate_tier": {
        "type": "select",
        "options": ["members", "paid"],
        "default": "members",
        "description": "Who can read an article after the gate closes. 'members' = any signed-in member (free or paid). 'paid' = paying members only; free members still see the gate."
      }
    }
  }
}
