{{!-- Traditions tab. The band-per-tradition markup that used to live
     here covered only the 69 curated English works and was generated
     by scripts/build-faith-received.mjs. The tab is now built by
     assets/js/faith-indexes.js, which indexes every collection —
     75,485 works — and resolves each one's tradition from its own
     catalogue, or from the collection's character where no field
     carries it.

     Left as a container so the index has somewhere to render, and a
     status line for the moment before the catalogues land. --}}
<div class="container">
  <p class="faith-scripture-status" data-faith-traditions-status>Loading traditions&hellip;</p>
</div>
